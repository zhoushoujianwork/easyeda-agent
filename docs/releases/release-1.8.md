# v1.8.0 发布准备

2026-09-27 用户确认进入发布流程。本次按完整验收范围准备 **v1.8.0**：
先通过同包基础 CLI，再从 ESP32 客户原始需求完成原理图和四层 PCB，最后独立复核及检查发布资产。
**当前处于准备阶段，尚未发布；完整用例尚未开始，基础失败项仍阻塞。**

## 实施顺序

1. 修复或明确纠正改名、区域/铺铜线宽、铺铜优先级的接口契约，并完成定向正负例及保存重载复测。
2. 固定同一个 CLI、daemon、连接器候选，逐项运行 B00–B10 并独立复核；旧版通过项不拼成新版通过。
3. 由无历史设计上下文的执行 Agent，只从 `esp32MiniRequire.md` 第一节开始选型、规划、
   原理图执行与检查，再完成 PCB 同步、四层配置和布局；不提供旧 BOM、网表或候选布局。
4. 展示通过连续两轮回读检查的 PCB 布局，由用户确认后进行整板布线、铺铜和 DRC。
5. 保存、真实重载、fresh 回读与独立复核全部通过后，统一正式版本，执行 `release-check`、
   `release-build`，再按[发布流程](../release-workflow.md)创建发布和核验远端资产。

复用测试工程与叠层 A 的既有选择继续有效，不重复确认；它不代替尚未生成的 PCB 布局确认。
本次还需覆盖局部修改、写前拒绝和中断恢复，不能只跑一个顺利创建的正向流程。

## 当前进度

| 项目 | 状态 |
|---|---|
| 发布前只读预检 | pass：当前 `1.7.1-dev.11` 三方同版，Web 4.1.60，目标工程与原理图路由正确 |
| 基础失败项 | #55 改名、#260 边界线宽、#261 优先级仍未解决；失败漏报修复不等于实际操作通过 |
| 最终候选基础 B00–B10 | not-run：候选尚未冻结，当前完整基础与历史失败见 [CLI Status](../cli-STATUS.md) |
| 完整高级用例 | not-run：基础门禁未通过；不续用旧选型答案作为新的端到端输入 |
| 正式发布 | blocked：不创建 tag，不上传未通过的正式资产 |

材料已建立：[基准](evidence/v1.8.0/baseline.md)、[用例](evidence/v1.8.0/test-cases.md)、
[执行报告](evidence/v1.8.0/test-report.md)。报告保留真实未通过状态，后续按证据更新。
修复范围见[版本评估](next-release.md)，当前使用范围仍以 [CLI Status](../cli-STATUS.md)为准。
