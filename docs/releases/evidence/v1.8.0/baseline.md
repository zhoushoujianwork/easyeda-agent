# v1.8.0 测试基准（准备中）

[发布主文档](../../release-1.8.md) · [测试用例](test-cases.md) · [执行报告](test-report.md)

## 输入与范围

2026-09-27 用户确认进入此前建议的 v1.8.0 发布流程。本次采用默认 schema 1 完整验收范围，
不沿用 v1.7.0 的基础 CLI 单独发布范围。正式候选尚未冻结，当前文件不证明已开始或通过完整用例。

唯一客户需求是 [`esp32MiniRequire.md` 第一节](../../../../esp32MiniRequire.md#一客户原始需求)。
按原始 UTF-8 字节截取 `source[start:end]`：`start` 为标题 `## 一、客户原始需求` 的起始位置，
`end` 为其后第一次字节序列 `\n---\n---\n` 的起始位置（不含该序列）。
截取结果为 **996 字节**，保留 Created by 行末一个 LF，排除分隔线前空行的额外 LF；
该段 SHA-256 为 `e62ebb05ecedb82e103d81b31da2bbf26aefe0669e580961c3aae87fef7d74d0`。
整份文件在准备提交 `963f717` 的 SHA-256 为
`b83134943f9d834e4d81a483551109b7d29db61009ff0ec6acfed4e020ad9f1d`。

完整 E2E 执行员使用无历史设计上下文的新 Agent；只提供原始客户需求、公开 Skill、
目标环境及已经确认的用户选择，不提供第二节 runbook、旧选型、BOM、器件 UUID、网表、
布局或旧候选报告。测试工程/文档 UUID 是环境路由信息，不是预制器件答案。
旧 A00 测量仅供诊断参考，不作为本次从需求到成品的输入或通过证据。

既有“复用＋A”决定保留：复用专用基础测试工程；TOP 元件与信号、内层 GND PLANE、
内层 3.3V 电源铺铜（SIGNAL 类型）、BOTTOM 以 GND 为主。来源见
[已确认范围](../../../reviews/2026-09-26-advanced-cli.md#已确认范围)。PCB 布局仍需另行确认。

## 环境与起点

- 源码准备基线：`963f717`，分支 `dev`；正式候选与发布资产身份待冻结。
- 实际预检运行包：CLI / daemon / connector 精确 `1.7.1-dev.11`，Web EasyEDA Pro `4.1.60`。
  `07c3060..963f717` 的 `cmd/`、`internal/`、`extension/src/`、连接器版本文件及预检脚本无差异。
  此事实仅说明当前仍是已有实现，不证明已修复或重新验证历史失败项。
- 目标个人工程：`ceshi-cli-basic-20260924`（`0f46d4361c4741a9ab7a9ed62164ca9b`）；
  原理图 P1 `e453c0063919726b`，PCB `3037fc1dfa4965d2`。
  原名 `ceshi` 的 AT32F415 工程不是空白测试板，不得清空。
- 当前窗口：`be65ae0d-4680-41f1-b94d-681fd7eb653b`。重连后必须重新通过 health 定位，不能把窗口 ID 当永久身份。
- 上轮已恢复空白基线并保存重载，见 [dev.11 清理记录](../../../reviews/2026-09-26-v1.7.1-dev11-pour-live.md)。
  本次仅重新核对原理图只读路由；未重采 PCB 完整对象、规则与叠层，不把历史快照冒充新基线。
  第一次工程写入前，须重新冻结双页完整快照及语义哈希，核对规则使用可恢复的自定义配置。

## 通过标准

本次基础准入按下述用户范围决定执行，再运行 [A00–A06](../../../cli-advanced-test-detail.md)
与本目录全部必测用例。严格 [B00–B10](../../../cli-live-test-detail.md)结果另列，不补签。
客户需求逐条映射、完整物理脚与网络/NC 对账、0 overlap、0 fatal、网络连通、四层电源树、
丝印极性、天线禁布区、四角 M3 固定及最终持久化均需真实证据。
局部修改须证明核心与专属外围整体跟随，范围外对象不变；拒绝和中断恢复另行验证。

所有写入必须来自参数化输入并经 typed CLI。保存、真实重载、新鲜回读和独立复核不可由截图替代。
依赖能力失败时停止对应下游，保存输入和错误，修复并重新验证；不得更换参数或降低标准补签原失败。

## 2026-09-27 用户接受的范围

用户明确移除未解决问题对本次流程的限制。#55 页面改名、#260 区域/铺铜边界线宽、
#261 铺铜优先级作为 v1.8.0 的已知限制，保持 issue open，但不再整体阻断高级用例和发布。
本次不要求显式写入这三个字段成功：页面用稳定 UUID；新设计没有特定边界线宽或覆铜排序要求时，
可在参数源中预先声明采用宿主默认值，再验证实际几何、铜、电气质量及保存重载。
不预设边界线宽只有显示影响；导线宽度、间距、连通、规则、客户需求和其他字段不在例外内。

准入采用 dev.9 已审阅的基础检查、dev.11 定向复测以及当前精确版本/目标与完整基线核对，
不要求先修三项或重跑基础全集。旧 B04/B08 fail 与 dev.11 全集 not-run 保留，
这不等于严格基础全通过，也不声明三项无功能影响。
具体请求不符仍非零退出并停止依赖步骤；旧失败不能改参补签，新缺陷不自动豁免。
正式发布仍要求本目录必测用例和独立复核完成，发布资产中的本文件保留上述范围披露。
