# v1.7.1-dev.4 B02 daemon 重连

执行结论：**blocked**；独立复核确认 **blocked**（evidence_review）。

双页 typed save 均返回 saved:true，原 P1 完整 list 与 PCB 完整 dump 已冻结。2026-09-26 10:07:07 UTC 精确 SIGTERM 停止已核对的旧 daemon PID84508，同一 `/usr/local/bin/easyeda` dev.4 二进制随后启动新 daemon PID5912，未改包、connector 或工程。

首轮 12 次 health（10:07:35–10:07:58 UTC）及 10:08:47 UTC 的延迟观察中，dev.4 目标连接器均未注册。新 daemon 仅出现同项目旧 dev.1、无 documentUuid 的窗口 `5fd508c3-0ac2-4a34-a488-d34c828722a3`；该窗口没有被选择或操作。因目标缺失，重启后没有发送任何设计读写，不能完成 fresh 对象比较。此结论仅描述该有界观察区间，不将原因猜成宿主或产品已证实缺陷。

- 输入版本/目标与完整保存前快照：01–05。
- 停止旧进程的实际命令、UTC、退出码：06-stop-old-daemon.json。
- 新 daemon 启动 argv/哈希：restart-plan.json；运行 PID/工具 session43255：new-daemon-running.json。
- 13 次完整 health 回包：07-health-00..11.json、08-health-late.json。
- 逐条断言与窗口清单：assertions.json。

新 daemon 仍在运行，前台启动命令的 stdout/stderr/最终退出码将由 recorder 在进程停止后写入 06-new-daemon-exit.json；目前退出码为 null，不伪造已完成。manifest 只冻结已产生的执行资料，后续关闭进程的证据另行补充。

当前双页最后已知内容仍是已保存的基线；不存在本项新建图元。未用 GUI、web reload、同页 reopen 或 debug JS 恢复。B03–B10 未开始；高级仍被基础门禁阻断。现场独占权交回 root 调查重连；执行员停止全部 EDA 调用。

补充：为冻结完整启动/注册日志，已在 2026-09-26T10:10:11.848608+00:00 精确停止本项新 daemon PID5912，退出码 0。06-new-daemon-exit.json 与 daemon.stdout.log/daemon.stderr.log 已落盘；哈希见 log-finalization.json。当前 daemon 已停止，现场交回 root。上述有界重连观察与 blocked 结论保持。

独立报告：`artifacts/cli-reconnect-fix-20260926/review/B02-independent-review.md`，
SHA-256：`5ed4879ec2d5c760ec72a7af079ef4d07673294ff3c21bfd83c26bb2d617e588`。
