# 基础 CLI 失败项调研与修复

## 当前结论

已用“官方契约 → 单变量现场对照 → 修复 → 离线负例 → 保存重载复测”完成五类缺陷修复。
本轮是失败项修复验证，**基础 B00–B10 总门禁仍未通过，高级 CLI 未启动**。
运行环境为现有内置浏览器 Web EasyEDA Pro 4.1.60；最终连接器为本地开发版 dev.20，没有正式发布。

## 哪些是我们的问题，哪些仍不确定

| 失败现象 | 调研结论及处理 | 本轮证据 |
|---|---|---|
| 原生 net_label 返回空对象 | 宿主实际上已新增可见 Name 属性；连接器错误地等同“未写入”。改为核对新增属性、父导线、网名、可见性与上下文；无法确认时保留部分写入证据并非零退出。 | [dev.19 完整连接原语](2026-09-24-v1.6.0-dev19-B06-repair.md)、[dev.20 标签复测](2026-09-25-v1.6.0-dev20-B06-repair.md) |
| 禁铺铜区域名称为 null | 官方规定名称只用于 follow-rule，旧测试参数有误，连接器又回显输入造成假成功。已修正示例、写前拒绝无效名称并回读真实字段。 | [dev.19 调研及配置恢复](2026-09-24-v1.6.0-dev19-B08-repair.md)、[dev.20 区域复测](2026-09-25-v1.6.0-dev20-B08-repair.md) |
| 过期 PCB ID 修改报 isAsync | 我们缺少写前 ID 检查。现在 fresh inventory 不可读或 ID 不存在均停止，不派发 modify。 | [B10 修复](2026-09-24-v1.6.0-dev19-B10-repair.md)、[dev.20 删除后负例](2026-09-25-v1.6.0-dev20-B07-repair.md) |
| PCB 实际是 R9，放置回包却是 R1 | 原创建对象 getter 保留旧值。改为按新 ID 从 fresh inventory 返回位号和 uniqueId；不匹配时 partial。 | [dev.20 B07](2026-09-25-v1.6.0-dev20-B07-repair.md)：位号、链接键及两焊盘网络保存重载一致 |
| B05 cmdKey / 首次 NC 清除未持久化 | 多组隔离实验及 dev.19 连续路径通过，未再出现；触发条件仍未知，不能归咎于某次网表读取，也不能称已修好。 | [B05 调查](2026-09-24-v1.6.0-dev19-B05-repair.md)；旧失败保留 |
| 整页 reload 在 30 秒预算前失败 | dev.20 首次刷新捕获宿主恢复页签期间切换目标，旧等待固定次数且未核对每次响应上下文。已补剩余预算、组件基线、上下文复核及最多一次 typed 恢复。 | [刷新单项报告](2026-09-25-v1.6.0-dev20-reload-repair.md)：新 CLI 原理图 5.1s、空 PCB 6.1s；初始失败完整保留 |

## 门禁还缺什么

- B05 历史间歇异常仍未定位；成功复测只证明这次路径可用。
- dev.20 没有重跑全部 B00–B10，不能把旧版 pass 合并成新版总通过；B03 真实团队创建及 daemon 存活但无 connector 的独立负例仍缺覆盖。
- B08 旧“预设首次转自定义配置”的孔间距 0.3 → 0.2999994 mm 变化仍保留。此次从已冻结自定义配置11开始的线宽修改、恢复和 2→4→2 层对照完全一致，并未补签初始化路径。
- 所有记录仅涉及基础动作；求解、Compose/Apply、全局布线和 DRC 业务结果不参与这些判定。

## 前置条件、步骤与完整性

各单项报告列出工程/文档身份、每条调用的耗时与退出码、持久化对账、清理和未覆盖范围。
原始记录位于本机 `artifacts/cli-basic-fixes-20260924/`，保留所有失败尝试和成功复测；没有覆盖旧回包。
临时器件、导线、标签、区域及两张实验页已按 ID 清理并保存重载；保留专用夹具工程及默认 P1/PCB1。
独立复核员只做离线证据核对，同一 Web 窗口始终串行访问。

## 仓库验证与版本身份

- 连接器全量测试 **543/543**；Go 全仓 `make test` 全部通过（app 41.135s）。
- `make local-build VERSION=v1.6.0-dev.20` 的 TypeScript 类型检查、五平台编译、包检查通过；`make skill-check` 通过。
- 基础 CLI 契约检查通过：75 个帮助、160 个 action、104 处 Skill 引用；检查期间审计文件没有变化。
- `make local-check` 为 **READY**：安装的 CLI、Skill、daemon、connector 与本地开发包匹配。此检查与 B00–B10 业务门禁分开。
- 最后新增 reload 修复只更换 CLI/Skill；dev.20 已导入连接器的 `dist/index.js` 与 manifest 哈希保持不变。各阶段 CLI 和最终产物哈希已归档，不能仅靠相同 dev.20 标签混用阶段证据。
- 新放 R8 的首次前后回读出现空 uniqueId → gge1；第二次完整器件记录一致。该观察与归因限制见刷新报告，没有忽略首次差异。
- 修复基于 `9917e824`；最终本地提交及验证结果随交付报告提供。没有推送或发布版本。


## 清理、独立复核与耗时

- 已通过 typed project.open 回到原工程 / P3，fresh health 精确确认工程与文档。与上一轮原 P3 最终基线比较：组件 ID 和所有已测字段一致，导线一致，属性非 ID 字段多重集合一致；本轮额外读出的引脚字段没有旧基线，不冒称已比较。
- 独立复核确认 dev.19 B06/B08/B10 和 dev.20 B06/B07/B08/reload 原始证据；单项 manifest 全部匹配。最终总 manifest 还涵盖导出文件、版本哈希和清理记录。
- 本轮审计区间 UTC 15:30:50–16:29:18：墙钟 **58.47 分钟**，daemon 执行 **103.74 秒**，其余约 **56.74 分钟**为调研、改码、构建、轮换、分析和报告等开销。成本台账已记录；token 未记录。不是全程在等待 EDA。
- dev.20 B07/B06/B08 各项 CLI 合计 **8.304 / 9.331 / 8.217 秒**；整页刷新实测 **5.1–6.1 秒**。审计中的失败包含故意负例，不能当产品故障率。
- 首次 health 多传 --json、首次返回原工程缺 --window 属于测试调用参数错误，均在派发前拒绝并修正；原始记录保留，没有算成产品缺陷。
