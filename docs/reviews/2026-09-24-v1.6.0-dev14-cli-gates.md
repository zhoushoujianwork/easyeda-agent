# 2026-09-24 `v1.6.0-dev.14` 基础 CLI 门禁复测

## 范围与结论

按[基础 CLI 真实链路用例](../cli-live-test.md)复测本地 `dev.14` 候选。首包源码基于
`92ff171`；命令契约修复包基于 `bf355d7`。Web EasyEDA Pro 为 `4.1.60`，目标测试工程为
`4037dd22434c412fa75d7f2d855a2ab4`。**离线构建、`dev.14` PCB/P3 只读链路及
修复后的错误子命令门禁通过；完整重连 C00R 为 `blocked`。** P1/P2 求解耗尽候选预算，
P3 严格原理图 gate 因 6 条 DRC warning 正常返回 `fail`；完整 CLI 链路和固定 E2E
均未通过验收。

## 已执行

| 检查 | 实际结果 |
|---|---|
| Go、连接器与协作检查 | `make test` 通过；连接器 517/517；`npm run typecheck`、`make skill-check`、`make agent-check`（13/13）、`make lint-test` 均通过 |
| 本地候选包 | `make local-build VERSION=v1.6.0-dev.14` 通过，生成已校验的 CLI、Skill 和 `.eext`；未打标签或发布 |
| 升级前保存 | 旧版 `dev.13` 的 P1/P2/P3 均 `saved:true`。PCB1 首次 `saved:false`，核对页 UUID 后再次 typed 保存为 `saved:true`；不能把首次返回计作成功 |
| 旧版真实只读链路 | 对 PCB1 的 `cli-live-smoke.py` 为 `pass`：CLI/daemon/connector 同为 `dev.13`，工程/页身份和 `pcb list` 回包匹配 |
| 新版安装态门禁 | CLI/daemon/Skill 切为 `dev.14` 后，连接器先缺席，随后旧 `dev.13` 重新连接；`health.versionGate=block`，`update --check --exit-code` 为 `NOT READY`（退出码 10），只读预检为 `blocked` |
| 新版同版链路 | 用户导入新 `.eext` 后，`health` 显示 CLI/daemon/connector 精确同为 `dev.14`，`update --check --exit-code` 为 `READY`；typed 打开测试工程及 PCB1/P3，并分别运行 `cli-live-smoke.py`，两页均为 `pass` |
| P3 原理图 gate | 默认 gate `pass`：layout-lint 0 overlap、0 pin-coincidence；check 0 error/fatal；bridge-check 0 short；DRC 0 fatal/error、6 warn。`--strict` 非零退出且 `verdict:fail`，阻塞项是 6 条 warning；检查器自身均完成，没有误报 `blocked` |
| daemon 重连门禁 C00R | 首轮重启后旧 `1.2.8` 扩展抢先接入；停用旧扩展后一次受控重启约 30 秒自动接入原 P3 页。但补齐全部测试页 `saved:true`、P3 完整前快照及重启命令记录后，再次重启 daemon，90 秒内 `health.windows` 始终为空，无法 fresh 对账，整项 `blocked` |
| C01 命令契约 | 安装修复包的 `project/doc/sch/pcb/lib/bom/apply/audit --help`、所选子命令 `--help` 与 `actions` 均可用；无效 flag 非零退出。首包的 `sch/pcb/project <不存在的子命令>` 错误地退出 0；修复包的三类无效子命令均退出 1，裸 `sch` 帮助仍退出 0。修复提交为 `bf355d7`；本轮只证明这些子项 |
| C11 错误页保护负例 | `sch save --doc 0000000000000000` 在页查找时退出 1；P3 前后 `sch list --include-wires --include-page-primitives` 完整 result 规范化哈希同为 `f25ce9ec03308c4e028d7b285195800720603d76da6f25dcce4351df29ab4165`。该单项通过，其余 C11 负例未运行 |
| P1 冻结输入负例 | 原始 SHA-256 `9b8ae6e81044ca407d0818d189ca486fbf53fb2d08965650aedbdba8bd7dd0c6`；20 万候选耗尽，非零退出、报告 `failed`，无布局输出 |
| P2 冻结输入负例 | 原始 SHA-256 `3430fa10a371f22410ab64afeb1867793bf73985d25acc6ae7b00cc2faacb775`；10 万候选耗尽，非零退出、报告 `failed`，无布局输出 |
| P3 冻结输入正例 | 报告 `sourceSha256` 匹配原始输入，`status:planned`，完整布局文件已生成 |

求解器输出保存在本机忽略目录 `artifacts/cli-live-dev14-20260924/solver/`；P1/P2
均为 `candidate-budget-exhausted` 且 `globalInfeasibilityProven:false`。预算耗尽不能推断
全局无解，也不能把 P3 单页成功当作三页整体 Apply 的许可。

Codex subagent 独立只读复核了原始回包：PCB1 `pcb list` 为 0 个元件，因此该 smoke
仅证明链路/路由可用，不能记为 PCB 设计对象写读通过；P3 `sch list` 读到 U3/C8 等
13 个对象，但未包含导线，不能据此证明网络。P3 默认 gate 报告通过，严格 gate 的
6 条 DRC warning 阻断属于预期门禁行为，严格设计验收仍失败。后续补存两次 gate
的命令、UTC 时间、退出码、前后同版 health，以及求解器的原始输入和运行记录；
subagent 复算三份输入哈希，并确认 P1/P2/P3 复跑报告与首跑逐字节一致。
修复包的连接器 `dist/index.js` 与首包 SHA-256 同为
`d5ecce49d6e3daa741311dba51994c135f189c8550441ff229b28e22f3e2872e`，
因此本次只更换 CLI/daemon/Skill，没有再次导入扩展。

## 用例覆盖

| 用例 | 本轮状态 | 未完成的前置条件或步骤 |
|---|---|---|
| C00 | `pass`（P3 只读链路）；PCB1 仅路由 smoke | PCB1 为空，未证明 PCB 对象写读 |
| C00R | `blocked`（曾有一次同版同页自动恢复） | 完整前快照后的重启 90 秒无连接器，无法 fresh 复核语义不变 |
| C01 | 部分 `pass`；整项未完成 | 帮助、action 目录、坏参数和无效子命令已测；未逐项对照 Skill 和写入前后审计 |
| C02–C04 | `not-run` | 未创建独立空白工程和器件/连接写读样例 |
| C05 | `fail`（P1/P2 离线求解）；P3 离线候选 `pass` | P1/P2 候选预算耗尽；无完整 Apply journal |
| C06 | `fail`（P3 严格 gate） | 6 条 DRC warning；未做本轮 save→reload→fresh readback 与导出 |
| C07–C10 | `not-run` | 原理图未通过完整门禁，PCB1 为空，未获布局确认 |
| C11 | `not-run`（错误页保护单项 `pass`） | 其余负例未执行 |
| C12 | `not-run`（独立复核已做） | 无本轮临时设计对象清理、完整成本记录 |
| 固定 ESP32 E2E | `not-run` | 未从原始需求完成 S0–S6/P0–P10 |

因此 C00–C12 全链路的覆盖判定为**未通过**；单项软件门禁行为通过，不等于设计完成。

## 现场阻塞与续测

当前源码包连接器 UUID 为 `4dae27407c1d43be98e8e210d45fe587`；原先运行的
`dev.13` 安装项为 `e60502e753e5211b3c30133bf2348a93`。开发热更新只允许同 UUID
且旧 bundle 哈希精确匹配，故没有把新包写进旧安装项。用户已在 Web EDA 导入
`dist/local-v1.6.0-dev.14/easyeda-agent-connector.eext`，首次同版检查通过；重启 daemon
后出现另一个 `1.2.8` 旧连接器窗口；扩展状态调整后 `dev.14` 恢复，重连后 P3
smoke 再次通过，13 个对象的身份摘要与重启前一致。安装 CLI 修复包后的一次受控
重启约 30 秒自动接入 `dev.14`，目标工程/P3 页不变，身份 SHA-256 仍为
`5ee2281508a242c6df62d48ff073cfed6f72582a16bf0aaaf4c4d969cd8cda8f`。
但是随后按完整前置条件记录 P1/P2/P3/PCB 四页 `saved:true`、P3 完整对象快照，
再以显式命令重启，daemon PID `49112` 正常监听 60832，5/30/60/90 秒的 health
均无窗口。该轮未取得重启后的 fresh 对象，故 C00R 最终为 `blocked`；原因尚不能
在连接器、浏览器后台调度与扩展安装状态之间定位。
P1/P2 仍在写前失败，C05–C10 的后续写读步骤和固定 S0–S6/P0–P10 端到端保持 `not-run`，除非后续
候选从同一原始需求产生完整可验证计划。
