# 2026-09-24 `v1.6.0-dev.14` CLI 测试记录（基础/高级重分类）

本记录修订的是**验收归类与结论**，没有重新运行测试。首包源码基于 `92ff171`，
错误子命令退出码修复包基于 `bf355d7`；Web EasyEDA Pro 为 `4.1.60`，测试工程 UUID 为
`4037dd22434c412fa75d7f2d855a2ab4`。原始命令回包保存在本机忽略目录
`artifacts/cli-live-dev14-20260924/`，Codex subagent 已独立只读复核。

## 基础 CLI：门禁尚未通过

按现行[基础动作测试](../cli-live-test.md)判定，`dev.14` 只覆盖了部分基础链路。
基础 CLI 的结论**不受**下文求解器或 DRC 结果影响。

| 基础检查 | 已有证据 | 本轮结论 |
|---|---|---|
| 离线与安装包 | `make test`、连接器 517/517、typecheck、skill/agent/lint 检查和本地构建均通过；CLI/daemon/Skill 安装为 `dev.14` | 离线/打包子项 `pass`，不代替现场 |
| 版本、路由与读取 | 同版时 `health` 和本地更新检查为 `READY`；PCB1、P3 的 CLI→daemon→connector smoke `pass`。PCB1 列表为空，P3 读到 U3/C8 等 13 个对象 | 只读链路子项 `pass`；PCB 对象写读未证实 |
| 命令契约 | 首包无效 `sch/pcb/project` 子命令误退出 0；修复提交 `bf355d7` 安装后退出 1。8 个命令组与所选子命令 `--help`、159 个 typed actions、坏 flag 拒绝均有回包 | 已测解析器子项 `pass`；完整 Skill 对照及零写入审计未覆盖 |
| 单动作保护 | 错误 `--doc 0000000000000000` 的 `sch save` 退出 1；P3 前后含导线/页面图元的完整 result 哈希同为 `f25ce9ec03308c4e028d7b285195800720603d76da6f25dcce4351df29ab4165` | 该保护负例 `pass`，其他负例未运行 |
| 保存与重连 | P1/P2/P3/PCB1 均有 typed `saved:true`；重启前有 P3 完整快照。显式重启后 daemon 正常监听 60832，但 5/30/60/90 秒 `health.windows=[]`，没有 fresh 后快照；较早一次重启曾在约 30 秒恢复同版 P3 | 完整重连用例 `blocked`，较早成功不能补签本次 |
| 工程、页面及原语写读 | 未按专用空白工程逐项执行创建/查找、原理图器件与导线、PCB 器件与几何的增删改查、导出及清理 | 对应基础用例 `not-run` |

因此基础 B00–B10 **未全部通过**，不得进入高级 CLI 的现场验收。
旧 `dev.13` 扩展和另一个 `1.2.8` 扩展曾先后接入；同版连接恢复过，但最后一次完整
重连测试没有连接器窗口。当前证据无法区分连接器、浏览器后台调度和扩展安装状态的原因。

## 高级 CLI：保留观察，不作本轮验收

下面的调用在基础门禁完成前已执行。它们属于[高级业务用例](../cli-advanced-test.md)，
只能说明当时观察到的响应；不计入基础 CLI 结论，也不标为高级验收通过。

| 高级观察 | 原始结果与边界 |
|---|---|
| P1/P2 原理图布局求解 | 冻结输入 SHA-256 分别为 `9b8ae6e81044ca407d0818d189ca486fbf53fb2d08965650aedbdba8bd7dd0c6`、`3430fa10a371f22410ab64afeb1867793bf73985d25acc6ae7b00cc2faacb775`。20 万/10 万候选预算耗尽，非零退出、无布局输出；`globalInfeasibilityProven:false`，不能宣称设计无解 |
| P3 原理图布局求解 | 输入 SHA-256 `5a155f5d21f7a4891ae8dc2500d900965c9d9ca137f1c1fba557c6c2db2fb502`，离线报告 `status:planned`、有布局文件；`zoneReview=review-required`，未 Apply 或现场验收 |
| P3 设计质量 gate | 默认 gate 报告 `pass`；严格 gate 因 6 条 DRC warning 退出 1、`verdict:fail`。这是高级设计检查的回包，不是基础动作故障，也不等于完整设计验收 |
| 固定 ESP32 原始需求 E2E | 未从原始需求完成 S0–S6/P0–P10，状态 `not-run` |

求解器复跑报告与首跑逐字节一致，输入哈希由独立复核员重算；P3 gate 的命令、时间、
退出码和前后同版 `health` 也已补存。这些证据仍不能跨过基础门禁的缺口。

## 后续进入顺序

先依据[基础步骤](../cli-live-test-detail.md)补齐单动作写读、持久化、负例和重连，取得
B00–B10 全部 `pass`。随后才依据[高级步骤](../cli-advanced-test-detail.md)重新执行
规划、Apply、严格设计检查与固定 ESP32 原始需求 E2E；历史高级回包不能直接迁移为新验收。
