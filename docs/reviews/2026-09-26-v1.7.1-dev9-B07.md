# v1.7.1-dev.9 B07 PCB 容器与单器件操作

执行与独立复核结论：**pass**。使用用户已授权的原 PCB，不依赖 B04 失败的新页；B04 仍 fail，整批门禁未通过。

目标工程 `0f46d4361c4741a9ab7a9ed62164ca9b` / PCB `3037fc1dfa4965d2`、唯一 window `97085490-2293-4453-a995-bbfc5f898336`，CLI/daemon/connector dev.9 / Web4.1.60。起始完整含铜 dump 与 B04 恢复基线仅 capturedAt 不同；docs/list/board-info 已实际读取，空板无外形 partial 保留。

- 从当前 sch read 取得 R901 的真实链接键 `gge1`，双引脚 net=null；这是有意孤立的基础夹具，sch read 的 floating-pin/check.passed:false 如实保留，不冒充电气设计通过。原理图已保存。
- 当前 typed device/footprint get 验证 C25804 device `b948db94476e4027ac8953235755ec96` 对应源 footprint `50b4943912284dab97752312e589e9e2`（R0603）。add-component 传 device-library UUID，未拿实例 UUID 当库 UUID。
- 第一件 `0f70af20731a1574` 一次添加于 `(1000,1000) mil / TOP / 0°`，bindingVerified:true，真实位号 R901/uniqueId gge1。两个焊盘编号1/2、31.8×34 mil，均未分配网络，与原理图 null net 一致；不声称测试过命名网络赋值。
- 一次 move `dx=200,dy=100 mil`。fresh component、全部 pads、所属 silk/属性位置和 bbox 与参数计算严格相等，所有身份/非位置字段保留，数值容差为0。中断后另做 health+完整 dump，与成功移动快照仅 capturedAt 不同，未重复已成功动作。
- 一次精确 delete 第一件，fresh list为空、完整 dump 精确恢复起始空板（只差 capturedAt），semantic SHA仍 `41b23a1b3e7c54f623efa678dea430033717968444fefee90e1ccbfebce0d527`。
- 在上述闭环完成后，按最初已冻结参数另建 B09 夹具 `a664c10df925a5e9`，位于 `(1400,1000) mil / TOP / 0°`，R901/gge1。这是有意第二次正例创建，不是失败重试；绑定及两焊盘回读正确，显式 save:true，保存前后完整 dump 仅 capturedAt 不同。

所有阶段对完整支持的数据逐项比较：只排除精确目标器件和 CompID 明确归属的丝印/属性、采集时间/派生hash，以及随空板状态变化的 no-components 提示。规则、copperLayers、铜、其他对象和其余 partial 全相同；未写规则、叠层或铜。无外形限制一直保留，不宣称完成整板布局、边界检查、DRC 或 B09 持久化/导出。

30 条 CLI（含3条help）均exit0；214条audit全部ok，原追加行序保留，无 domain ok/verified=false。最终仍为原 PCB，daemon PID 2615 / session36166，原 P1 的 R901 已保存。未改源码或运行包。

[保留夹具与全量基线](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B07/retained-state.json)供后续 B09 使用；目前只保留这一 PCB 器件，导线/铜仍为空，不能以此补签 B09 的非空导线/铜持久化要求。B08–B10 未执行，现场交回 root。

原始证据：[B07](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B07)；[移动验证](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B07/move-verification.json)；[删除恢复](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B07/delete-verification.json)；[冻结清单](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B07/manifest.json)。

独立复核：`artifacts/cli-basic-dev179-20260926/review/B07-independent-review.md`，
SHA-256 `329151ca03a421550608288cd75bda7cc3a151a657065e4eaa5c2808c017e212`；
inputs-v2 SHA-256 `2617bda91579efab3157b73427f1adfd474efdaf4e354a7da5ab7d8b15558603`。
52 件冻结文件、290 项检查通过。完整 214 条审计与原始追加顺序相同，确认首件无重放、
删除恢复完整、新保留件保存后稳定。复核首轮把 CLI 派生的 bbox center 与原始 action
回包直接比较，后按源码中的精确转换闭合；原 v1/v2 与补充记录均保留，未修改现场回包。
本项仍保留悬空检查、无板框、staleRisk 和未重载/导出的限制，不补签其他编号。
