# B09 单项报告：保存、重载与导出

## 结论

`pass`（本轮夹具）：PCB 完整对象持久化一致；原理图的生成属性 ID 重建已有逐项对应、相同渲染和网表证据。未将旧版未解释差异自动归为通过。

## 前置条件与范围

- 2026-09-24，Web EasyEDA Pro 4.1.60；CLI/daemon/connector 标签 dev.18，CLI 含 `c6bdc05` 查找预算补丁。
- 本轮从源码 `402419c` 开始，后续提交仅更新测试工具与文档，没有更换运行时。
- 工程 `ceshi-cli-basic-20260924`，UUID `0f46d4361c4741a9ab7a9ed62164ca9b`；窗口 `b0e82c52-5c4a-44c1-ae80-f13a37d9cd1b`。
- 临时原理图页 `45355d82797ca512`；默认 PCB `3037fc1dfa4965d2`。
- 本项只有基础动作及直接回读；没有运行规划、求解、Compose/Apply 或 DRC 业务验收。

## 检查与实际结果

- PCB save 后真实 reload 用时 3.424 秒。组件、焊盘、板框、丝印、线/孔/区域/铜皮及规则完整 dump 除 `capturedAt` 外逐字段完全相同，`semanticSha256` 也相同；本轮没有隐藏丝印坐标变化。
- 原理图 save 后真实 reload 用时 2.239 秒。组件、引脚/网络、导线、其他页面图元及 context 完全一致。
- 133 条属性使用完整非 ID 字段组成的多重集合逐一对应，保留重复属性计数；30 条生成属性 ID 更新，父对象、键、值、坐标、可见性、字体等字段全部一致。`verification.json` 保留 133 条映射，不是按数组顺序比较，也不把重复的 parent/key 静默覆盖。
- SVG 前后各 341 个 XML 元素，树结构、文字及所有非 ID 渲染属性一致；38 个导出元素 ID 改变，每个在文件仅出现一次，无引用关系，故不影响渲染。网表前后 6686 字节、SHA-256 完全相同，电气结果一致。
- 已实际校验文件大小和 SHA-256：SVG 各 65893 字节，网表 6686 字节，BOM CSV 340 字节，PCB DSN 1924 字节。原始响应含完整 artifact 路径和哈希，`verification.json` 再次汇总校验结果。
- 判据只证明已有对象可靠保存；B05/B06/B08 的写入问题仍独立失败，不被本项成功抵消。

## 原始证据

本机目录 `artifacts/cli-basic-auto-dev18-20260924/B09/`。每份命令 JSON 保留完整参数、
UTC、退出码、stdout/stderr；`evidence-sha256.json` 保存本项 JSON 哈希。以下时间为 UTC。

| 文件 | 开始 | 秒 | 退出码 |
|---|---|---:|---:|
| `bom-export.json` | 15:09:39 | 0.039 | 0 |
| `pcb-dump-after.json` | 15:09:25 | 0.212 | 0 |
| `pcb-dump-before.json` | 15:09:21 | 0.328 | 0 |
| `pcb-export-dsn.json` | 15:09:26 | 4.543 | 0 |
| `pcb-reload.json` | 15:09:22 | 3.424 | 0 |
| `pcb-save.json` | 15:09:22 | 0.034 | 0 |
| `sch-image-after.json` | 15:09:37 | 1.858 | 0 |
| `sch-image-before.json` | 15:09:33 | 1.501 | 0 |
| `sch-list-after.json` | 15:09:37 | 0.369 | 0 |
| `sch-list-before.json` | 15:09:30 | 2.369 | 0 |
| `sch-netlist-after.json` | 15:09:39 | 0.031 | 0 |
| `sch-netlist-before.json` | 15:09:34 | 0.049 | 0 |
| `sch-reload.json` | 15:09:35 | 2.239 | 0 |
| `sch-save.json` | 15:09:34 | 0.021 | 0 |

## 清理与边界

临时测试页与 PCB 对象尚在，立即交 B10 做写前拒绝、完整快照对照与精确清理。
