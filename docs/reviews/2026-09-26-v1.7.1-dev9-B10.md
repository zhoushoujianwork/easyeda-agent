# v1.7.1-dev.9 B10 保护负例与清理

执行与独立复核结论：**pass（本项）**。四个负例都按预期拒绝，完整对象未变；本批临时器件与几何已精确删除并保存重载，原工程恢复空白基线。B04、B08 保持 fail，高级验收仍未运行。

## 目标与前置

CLI/daemon/connector 为 `1.7.1-dev.9`，Web `4.1.60`；主 daemon PID2615，唯一窗口 `97085490-2293-4453-a995-bbfc5f898336`。授权工程 `0f46d4361c4741a9ab7a9ed62164ca9b`，P1 `e453c0063919726b`，PCB `3037fc1dfa4965d2`。本项安装 CLI SHA256 为 `149643c600c476e6b87e973722c5ff6f9a8f6bdefdee5ba92c7732b91899e4bf`，包源码对应4e6e56b；其后报告提交未改变该包。本项没有改变版本、主daemon或connector。

开始05完整PCB dump与B09最终数据除capturedAt外相同；负例后38完整SCH result与B09最终完全相同。全部动作来自 `parameters.json`，每次CLI保存UTC、参数、退出码及完整stdout/stderr。开始/结束health均为同一目标，最后active为原P1。

## 负例结果

| 负例 | 实际拒绝与完整范围核对 |
|---|---|
| 错误文档 | 09传入不存在的专用doc名，exit1；只调度document.current与文档inventory四条只读动作，无写动作。10全PCB dump与初始完整相同（除capturedAt，semantic hash相同）。 |
| 过期器件ID | 11使用B07已删除的首件ID `0f70af20731a1574`，06当前fresh inventory也证明不存在。exit1、PRECONDITION_REFUSED。typed modify请求进入handler，但在官方modify前拒绝；12全PCB不变。 |
| 无效参数 | 13的 `--x invalid-number` 由Cobra数值解析拒绝，exit1、audit零调度；14全PCB不变。 |
| 缺失connector | 同一安装binary临时启动51901端口、PID46588，health确认windows=[]，禁用Skill更新和autosave。17命令exit1；仅两条document.current返回NO_CONNECTOR，无写动作；主daemon未停止，19全PCB不变。 |

过期ID的官方modify零调用依据是本批同包handler与构建回归：冻结的 `stale-modify-handler.ts.txt`、`stale-modify-test.ts.txt` 及 `same-package-stale-guard-regression.log`。该测试对空/不可读inventory断言writes=0；不是B10新跑的单测，也不借旧版本现场pass代签。源码全文件SHA与 `git show 3eba02c:<path>` 严格绑定，见 `source-commit-binding.json`，不引用后续修改中的工作树。

隔离daemon由finally精确SIGTERM该PID，记录器exit0，端口释放。工具PID文件为全局位置，预先保存原字节 `2615\n`；仅在文件缺失或仍为临时PID时恢复原字节。本次满足条件，18/49主health仍PID2615、同窗口，最终PID文件SHA与原始一致。`isolated-daemon-plan.json` / `isolated-daemon-cleanup.json` 保留过程。两条NO_CONNECTOR是CLI文档保护的只读请求，不是重试写入，也不是主connector的执行。

## 精确清理与恢复

PCB依次删除track `57f73800497cf335`、via `baa92b00593548a8`、R901 `a664c10df925a5e9`；全量读取确认板上仅有本批outline `6d6eb5211934cfc3` 后typed清除。每步full dump验证范围仅为对应对象及其派生统计、器件自有silk或空板可用性说明，未改其他几何。无region/pour残留，B08失败对象已在该项清理。

删除测试铜后，29网络为空，30完整规则已严格恢复原自定义配置11、netRules=[]；31完整260层与原值全等。因此没有多余规则写入。32 saved:true → 33 reload → 34全dump → 35规则/36层回读再次完全闭合。最终PCB与B00完整原始空板数据仅capturedAt不同，semanticSha256为 `41b23a1b3e7c54f623efa678dea430033717968444fefee90e1ccbfebce0d527`。原空板partial（无器件、无板框）保留，不宣称完成板尺寸或全板几何检查。

SCH依次精确删除flag `5f45ee75796c3c14`、wire `c92bea22eb4b3bf6`、R901 `577f766eaf8a9e3e`。每次删除回包requested=total=1，deletedIds精确匹配；全类native对象证明只删指定对象及所属属性，留下对象所有字段和ID完全相同，没有新增对象。顶层图框自动更新时间单独记录。

45 saved:true → 46 reload → 47 fresh后，仅剩原sheet `52b6eb19efc39df2`，无设计器件/导线/网络标记。完整result对B00及B03原工程空白基线都只有 `/components/0/otherProperty/@Update Time` 从18:00:46变为22:45:34；原图框native属性和几何完全相同。删除过程中的pin2投影网名曾保留先前捕获值，未将其当作“电气网已清空”的证明；最终器件本身已删除且完整保存重载。

B03个人测试容器 `9d80866defa649a48bbd7a808676160d` 保留，当前无typed项目删除能力；B04临时P2此前已删除且容器原P1/PCB保存重载恢复，本项没有重新打开它。其他工程与AT32F415没有访问或改写。清理结论是授权原工程与本批对象范围，不承诺删除整个容器。

## 审计、验证和交接

49个CLI进程中45次exit0、4次预期exit1（包含三次help及隔离daemon启动/退出）；04 `sch delete --help` 实际仅返回parent帮助，不冒充该命令已实现，清理使用公开 `sch prim-delete`。audit共358条，355条ok、3条预期错误（1 PRECONDITION_REFUSED、2 NO_CONNECTOR），按原始追加顺序冻结。没有未说明失败或负例后重复写入。

UTC为 `2026-09-26T14:41:36.808914+00:00` 至 `2026-09-26T14:46:25.151909+00:00`；墙钟288.342995秒，CLI累计13.417秒。token未记录。详见command-index、audit-summary与每条完整回包。

离线验证器重算110项检查通过：

```bash
rtk python3 artifacts/cli-basic-dev179-20260926/B10/verify-evidence.py --check
```

最终判据在 `verification-v2.json`。首次派生文件保留为 `verification.json`；其重算比较曾因Python集合输出顺序不稳定失败，v2将删除属性按ID排序后全量复算通过；未修改现场原始文件或重放动作，见 `evidence-notes.json`。外部基线和验证器依赖在 `dependencies.json`，所有本项证据由 `manifest.json` 冻结。

现场已在49最后health后明确交回root，此后仅离线收尾；`final-state.json` 记录保存重载后的准确状态。B10已独立通过，不把本项通过用于覆盖B04/B08失败或放行高级。

[证据目录](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B10)。

独立报告 `artifacts/cli-basic-dev179-20260926/review/B10-independent-review.md`，SHA-256
`46e05bb7588d68a3a5ca9b6744733f313a1090975a467116508484240a1dc627`；最终输入清单 v3
SHA-256 `c9af74b6fff7a4b820a426ad50450351e133c36e13b725d218c2853e2566b8d6`。
90 件文件和 412 项独立检查闭合。全批独立索引 `review/B00-B10-independent-conclusions.json`
SHA-256 `39c977552e49d10e5f8612c22b4e7cebcaa1ee4824983bfb8609dee45c7afb43`：9 pass、B04/B08 fail。
