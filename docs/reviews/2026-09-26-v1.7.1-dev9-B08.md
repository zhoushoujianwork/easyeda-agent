# v1.7.1-dev.9 B08 PCB 几何与配置原语

执行与独立复核结论：**fail**；trial 清理独立 **pass**。原有 B04 fail 不变；本报告只判 B08，不放行高级、不判 B09。

两个区域和铺铜边界都请求 `--width 1` mil，fresh 实际为 `0.2` mil。无重试写入、无放宽宽度判据。失败对象已精确删除，全部试验图元清理后的完整 dump 恢复 B07 基线；随后按事先声明，仅新建成功类型的板框、单导线、过孔并保存重载，供 B09 使用。

## 环境与范围

CLI/daemon/connector 精确同包 `1.7.1-dev.9`，Web `4.1.60`，CLI SHA256 `149643c600c476e6b87e973722c5ff6f9a8f6bdefdee5ba92c7732b91899e4bf`。本批包来源提交 `4e6e56b`；执行阶段 Git 报告整合到 `817b24c`，未重建包。daemon PID2615，唯一窗口 `97085490-2293-4453-a995-bbfc5f898336`；前后 health 精确相同目标。

工程 `0f46d4361c4741a9ab7a9ed62164ca9b`（ceshi-cli-basic-20260924），PCB `3037fc1dfa4965d2`。原理图不操作。初始仅 B07 保留 R901 `a664c10df925a5e9`，(1400,1000) mil，实测两焊盘空网；原有配置名“自定义配置11”、2铜层、无板框、nets[]。原空板 partial 不充当已有板框能力。

参数来源为本次完整回读 `pcb-initial.json`、`03-config-before.json`、`04-stackup-before.json`、`05-layers-before.json`。`parameters-v1.json` 冻结接口范围、单位与相对 R901 锚点的偏移，`geometry-computed.json` 保存计算结果；未借用旧版坐标或 ID。测试网络 `BASIC_DEV179_COPPER` 由明确网名的单条 typed track 建立，33 fresh nets 返回它；R901 焊盘一直空网，不声称电气连通或设计 DRC。

## 逐项结果

| 子项 | 实际结果 | 证据 |
|---|---|---|
| config get/dry-run/set/restore | pass：Track min 6→7 mil，完整原规则恢复；配置名不变，#258 警告完整保留 | 03、15–21 |
| stackup | pass：2→4→2；只启用 Inner1/Inner2，再恢复；260层全部字段严格相同 | 04、22–27、81 |
| 板框 | pass：按相对锚点计算1600×1000 mil板框，宽1，中心线1100..2700/700..1700；清理还原 | 28–30、64–66 |
| track | pass：单件 ID、TOP/测试网、1700,1000→1900,1000、10mil；精确删除，无范围外差分 | 31–37 |
| via | pass：1900,1100，孔12/外径24mil、测试网；精确删除，无范围外差分 | 38–43 |
| no-pours region | **fail**：rule7/无名称/层和多边形正确，width1→0.2；verified:false/partial:true/exit1 | 44–49 |
| 具名 follow-rule region | **fail**：rule9/名称与几何正确，width1→0.2；verified:false/partial:true/exit1 | 50–55 |
| pour boundary | **fail**：测试网/名称/solid/层和边界正确，width1→0.2；create exit0没有证明请求已兑现 | 56–63 |
| 清理与保留 | trial全部清理后恢复B07完整dump；另建成功类型的新夹具，saved:true→reload→fresh | 66–88 |

规则写回共有22个未请求矩阵单元 `0.1759966 → 0.17599659999999998`，逐字段列于 `config-set-verification.json`；仅按原有 `8×Number.EPSILON×max(abs)` 相对机器舍入判据接受，零无绝对容差。重载后反向尾差见 `config-reload-rounding.json`，规则树最终与原始**严格相同**，不是工程尺寸容差通过。原始参数、单位、数组和键均保持。

Pour 的 `poured:false`、`available:true/count:0/poured:[]` 单列为实际材料化状态；未据此判定铺铜算法或 DRC 故障，未重建以强求通过。宽度不符本身已足以失败。区域两次 CLI exit1；pour CLI exit0但 fresh 差分失败，不能用外层成功补签。原因尚未知；当前现场包不修改。

## 恢复与保留状态

失败 ID：no-pours `5933b686f56359ce`、follow-rule `5ba4e549020fddd5`、pour `b0446ddde81e0873`；均先 fresh 确认再按 ID 精确删除。首次成功 track `bceb117b05d7bc12`、via `b55f37c535a668db`、outline `117830d845aadc0d` 也删除并验证。66 `pcb-all-trials-cleaned.json` 与本项初始 dump 除采集时间外完整相同（语义 hash也同）。

保留给 B09 的**新对象**：板框 `6d6eb5211934cfc3`；track `57f73800497cf335`；via `baa92b00593548a8`；原 B07 R901 保留。没有 region/pour/materialized poured。此新组来自 `retention-plan.json`，不是失败动作重试。最终 baseline `pcb-after-reload.json` SHA256 `c8e83adb2c171de64106ea3694985275ee3d7dd82b3501e32633c8ec34df3887`，全部 ID、参数与 B10 清理步骤在 `retained-state.json`。

规则名称、规则树、classes 和完整叠层已恢复。当前因明确保留测试铜，还保留一个名为 `BASIC_DEV179_COPPER` 的默认 netRule；74→80逐字段一致。**不声称最终 netRules 已回到原 []**。B10 删除测试铜后须确认该网络及默认绑定消失，必要时按冻结原配置 typed 恢复并完整对账。

76 saved:true，78 doc reload，79 fresh：组件/焊盘/导线/过孔/板框和可见位号逐字段相同。Footprint、Device 两个同 ID 属性的 X/Y 从1400/1000变0/0，其他字段相同；完整前后映射在 `verification.json`、四字段差异在 `reload-complete-diff.json`。bbox为null不能单独证明隐藏或渲染等价；本项保留未验证边界，**不补签 B09 属性实际位置、渲染/电气效果**。未执行整板布局、布线、DRC。

## 记录与判据

88条 CLI 命令（含7条帮助）：85 exit0、3 exit1。其中44/50是实际region失败；87是runner给health误加不支持的--window，CLI本地拒绝，88正确只读health闭合。该原始错误保留，无设计重试。初次离线严格配置全等断言未处理既有允许机器尾差的误停也保留，修正验证器而未重写现场动作。

UTC区间 `2026-09-26T14:11:16.187697+00:00` 至 `2026-09-26T14:22:13.416400+00:00`；墙钟 657.228703s，CLI进程累计 10.832000s，token未记录。620条原始 audit 均外层ok=true、全部精确目标窗口；**外层成功不等于domain verified通过**。audit按原追加行序冻结，未按ts排序。root后续14:22:50只读诊断不在本项区间。

实际命令、UTC、耗时和exit见 `command-index.json`；每条完整stdout/stderr仍在编号JSON。离线 `rtk python3 artifacts/cli-basic-dev179-20260926/B08/verify-evidence.py --check` 重算42项断言并保留3个真实宽度失败。无GUI/任意debug工程写，无源码或运行包变化。

已将现场独占权交回root，未执行B09。后续最小修复验证为处理区域/铺铜宽度契约和pour失败传播后，在新同包批次复验；本批原失败不覆盖。

证据目录：[B08](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B08)。
独立报告：`artifacts/cli-basic-dev179-20260926/review/B08-independent-review.md`，
SHA-256 `9e2754d963170ddfb2046b6b8f9723b2b6395938a873d782e4689fb6e18b7904`；
输入清单 SHA-256 `c52a384d5d331b94c9f4aba98a36722331bd757349b5a51722326a5f30ef646d`；
补充断言 SHA-256 `75840d608f483056015988aed626f04fb446990223598ab1813f78c82fa7d153`。
146 件冻结文件和 616 项独立检查闭合；88 条 CLI 与 620 条原追加审计精确匹配。
