# v1.7.1-dev.9 B04 页面容器与重命名

执行与独立复核结论：**fail**。精确清理和基线恢复另获独立 **pass**，不能补签本项通过。

新测试工程 `9d80866defa649a48bbd7a808676160d` 原 P1 和父文档经 fresh 核对后，只创建一次 P2 `713f21a758b1391a`。fresh pages、显式 doc open、完整 sch list 均确认这张新页，随后只执行一次重命名为 `CLI_BASIC_DEV179`。

`15-page-rename.json` 记录 CLI **exit 1**、顶层 **ok:false / EDA_CALL_FAILED**。宿主 **hostAccepted:false**，**verified:false**，官方回读仍为 **P2**。本次正确暴露失败，不能把错误处理改进视为重命名成功。未重试写入、未通过 GUI 或私有 JS 修复。

失败后立即停止设计动作。仅 fresh 精确核对新 P2 → 打开测试工程原 P1 → typed 删除唯一新页 UUID → fresh pages 确认只剩原 P1 → save/reload/fresh。测试 P1 完整 result/context 与 `09-test-p1-baseline.json` 严格相同。随后返回原工程 `0f46d4361c4741a9ab7a9ed62164ca9b`，原 P1 及 PCB 各完成 save/reload/fresh：P1 完整 result/context 与 `06-original-baseline.json` 全等，PCB 完整 dump 与 B03 恢复基线只差 capturedAt；空板 partial 保留。新个人容器和原 P1/PCB 留存，没有残留本次 P2。

按协调明确要求补做一次正常 typed debug exec 的只读宿主身份诊断，没有读写工程数据或停止 controller。结果是 visibility **visible**、标题 `ceshi-cli-basic-20260924 | P1.Schematic1 | 嘉立创EDA(专业版) - V4.1.60`，top href 指向原工程/P1；controller implementation `1.7.1-dev.9:cross-eval-v1`、connected=true、window `97085490-2293-4453-a995-bbfc5f898336`。href/title/frames/status 全量保留于 `16-readonly-window-identity.json`，供 root 与当前 IAB 只读身份交叉核对。

本项 33 条 CLI：32 条 exit 0，1 条重命名 exit 1。166 条 audit 中 3 条失败：1 条真实重命名失败，以及两次 doc reload 期间被处理的 document.current / No active document；后者不计成功，具体命令映射见 execution-summary.json。全部动作仅绑定上述同一窗口。本项结束时 daemon PID 2615 / session 36166，三方 dev.9，最终回到原工程 P1，B05–B10 尚未执行。本项失败阻断基础全通过及高级放行；后续独立基础项见[总表](2026-09-26-v1.7.1-dev9-basic-cli.md)。

原始证据：[B04](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B04)；[失败原包](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B04/15-page-rename.json)；[清理断言](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B04/cleanup/assertions.json)；[冻结清单](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B04/manifest.json)。历史失败证据未修改。

独立复核报告：`artifacts/cli-basic-dev179-20260926/review/B04-independent-review.md`，
SHA-256 `7ac855b18e776a278adf2947c779389eac0001482c6b9ae9abd0aadbc2ffbddc`；
输入清单 SHA-256 `9ac2088dc5d2514f769ed66899588bf93177bc7b3fd3702338a33f1d313fe27c`。
43 件冻结文件、168 个独立断言匹配。三次真实 close/reopen 均有审计支持。
13:24:57 的身份诊断只证明清理后的原 P1 可见且 controller/window 一致，不能倒推
改名瞬间 P2 的可见性，也不能单独作为外部 IAB tab 的绑定证据。改名请求本身由冻结窗口、
目标 UUID、fresh 页清单和单次 mutation 回包定位；具体拒绝原因仍未知。
