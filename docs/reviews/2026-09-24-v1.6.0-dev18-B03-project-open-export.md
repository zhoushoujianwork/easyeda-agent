# B03 单项报告：工程打开与原生导出

## 结论

**打开与导出子项 `pass`。** 原工程四个打开文档均先 typed 保存。跨工程打开新回归
容器用时 4.323 秒，随后读取工程身份与文档清单；指定 P1 再打开并核对工程/页身份
用时 1.280 秒。保存后导出原生 `.epro2` 用时 0.420 秒，ZIP 条目与 CRC 校验通过。

结合前两份报告，当前个人空间的创建、查找、打开、导出已完成真实调用。
显式个人归属 UUID 的团队创建路径仍有失败记录，真正团队工程未验证；本报告不将
B03 的所有可选路径标为通过，也不代表导出的工程已重新导入验证。

## 前置条件

- 源码 `402419c`；CLI/daemon/connector 构建标签 dev.18，CLI 含 `c6bdc05` 查找修复；
  Web EasyEDA Pro 4.1.60。没有本轮代码或插件更新。
- 窗口 `b0e82c52-5c4a-44c1-ae80-f13a37d9cd1b`；原工程
  `4037dd22434c412fa75d7f2d855a2ab4`，P1/P2/PCB1/P3 保存均 `saved:true`。
- 新工程由前项创建并完整回查：`ceshi-cli-basic-20260924`，UUID
  `0f46d4361c4741a9ab7a9ed62164ca9b`，保持为后续基础测试夹具。

## 真实步骤与证据

原始目录 `artifacts/cli-basic-auto-dev18-20260924/B03/`；每份 JSON 包含完整命令、
UTC 起止、耗时、退出码和原始回包。

| 文件 | 操作 | 结果 |
|---|---|---|
| `save-<docUUID>.json`（4 份） | 逐页 `sch/pcb save --project <原工程> --doc <页>` | 均退出 0、保存成功。 |
| `open-new.json` | `project open --window <窗口> --project-uuid <新工程> --allow-discard-unsaved` | 4.323 秒、退出 0；`opened/verified:true`，工程 UUID 精确命中。 |
| `info-new.json`、`pages-new.json`、`docs-new.json` | 当前工程、原理图页和文档清单 | 新工程名称/归属正确，P1 与 PCB1 存在。 |
| `open-with-page.json` | 同一打开命令加 `--page-uuid e453c0063919726b` | 1.280 秒、退出 0；`documentVerified:true`，页 UUID 精确命中。 |
| `save-new-p1.json` | 显式保存新工程 P1 | 0.022 秒、`saved:true`。 |
| `export-source.json` | `project export-source --uuid <新工程> --out <证据目录>/new-project.epro2` | 0.420 秒、退出 0；原生文件存在。 |
| `export-validation.json` | 本地 ZIP/CRC、大小、哈希验证 | 11053 字节，3 条目，CRC 正确；`restoreVerified:false`。 |

导出 SHA-256：`c269c871ea609c97a5b6d0974985a43e06f65cc8e8f916fb3ec8d36d4aedb0ef`。
新 P1 `e453c0063919726b`，父原理图 `8e23e98d3613f6cd`，PCB1 `3037fc1dfa4965d2`。

## 当前状态与后续

原工程已保存，新工程 P1 为当前页。没有执行设计写入、手工开关工程或 GUI 恢复。
新工程保留用于 B04–B10；下一项验证独立临时页的新建、改名、保存和真实重载。
