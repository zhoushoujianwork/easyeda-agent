# B07：PCB 放置回包使用 fresh 位号与 uniqueId

结论：**本次针对性子项通过；不补签整个 B00–B10 门禁。**

## 前置条件

- Web EasyEDA Pro 4.1.60；CLI/daemon/connector dev.20。
- 测试工程 `ceshi-cli-basic-20260924`（`0f46d4361c4741a9ab7a9ed62164ca9b`），P1 `e453c0063919726b`，PCB1 `3037fc1dfa4965d2`。
- 同窗口串行，所有工程动作经 typed CLI；实际命令、时间、完整回包保存在本机 `artifacts/cli-basic-fixes-20260924/dev20-B07/`。

## 调研、修复和结果

- dev.19 请求 R9 后实际列表为 R9，create 对象 getter 仍返回 R1；我们的回包错误地使用了旧对象。
- dev.20 以新建 ID 在 fresh getAll 中定位器件，回包使用真实 designator / uniqueId。不能回读或身份字段不符时保留 ID、partial，并让 CLI 非零退出。
- 本轮请求 R9 / CLI_FIX_R9，放置回包与 fresh list 一致；保存重载后完整器件（包括两焊盘网络）逐字段相等。
- 正常修改 X=3520 成功；精确删除后对同 ID 修改被 PRECONDITION_REFUSED 拒绝。
- 离线负例另覆盖位号成功但 uniqueId 丢失、全部修改丢失、fresh inventory 不可读。bindingVerified 仅核对请求的身份字段；两焊盘网络由本次现场独立 readback 验证。

## 单项步骤与耗时

| 证据/步骤 | 秒 | 退出码 |
|---|---:|---:|
| `add.json` | 0.508 | 0 |
| `baseline.json` | 1.574 | 0 |
| `clean-reload.json` | 3.041 | 0 |
| `clean-save.json` | 0.039 | 0 |
| `delete.json` | 0.054 | 0 |
| `final.json` | 0.143 | 0 |
| `fresh.json` | 0.034 | 0 |
| `modified.json` | 0.025 | 0 |
| `modify.json` | 0.058 | 0 |
| `persisted.json` | 0.027 | 0 |
| `reload.json` | 2.728 | 0 |
| `save.json` | 0.037 | 0 |
| `stale-modify.json` | 0.036 | 1 |

`sha256.json` 同时核对命令回包和导出文件。退出码 1 的负例需与预期拒绝原因一起判定。

## 清理与范围

R9 已删除；保存重载后完整 PCB dump 与基线除 capturedAt 外完全相等。本项只重测新发现的放置回包及过期 ID；不是 B07/B10 所有用例的完整重跑。
