# 原理图删除修复：2026-09-26

**已修复删除回读误报和错误级联；网表导出后的间歇删除故障已有本地候选，尚未完成现场验证。**
旧版对照确认：只导出网表就能复现故障，不需要器件身份查询或工程归档。

本地 `1.7.1-dev.1` 包增加导出后恢复原标签输入焦点，并将删除结果分成已删、存活和未知。
回读失败不再虚报成功，不再据此清理连线或分组；CLI 输出最终复核结果并保留每轮证据。
离线测试、构建和独立代码复核已通过。

候选已经安装。切换回专用测试工程时官方 `project.open` 超时，当前未获得目标文档身份；
已停止现场写入并请求用户确认 Web 测试页已加载。此前测试件已精确清理、保存重载，
运行包切换前已保存两个测试工程的全部六份文档。

现场重新连接后，先验证原始失败序列及保存重载，再恢复“复用＋A”的高级验收。
**A00 仍 blocked，A01–A06 仍 not-run。**

命令、对照结果和候选哈希见[详细记录](2026-09-26-sch-delete-fix-detail.md)。
