# 高级 CLI 续测：2026-09-26

本轮已完成同版基础证据复核、目标工程只读预检和离线回归，修复了多窗口导致的预检误拦。
**高级现场 A00–A06 尚未通过**：测试工程选择和四层叠层解释待确定，尚未开始设计写入。
命令、证据和边界见[详细记录](2026-09-26-advanced-cli-detail.md)。

当前现场 CLI、daemon、目标连接器为 `1.6.0-dev.21`，Web EDA 为 `4.1.60`。
不是 `1.7.0` 现场复测；源码工作树的正式版本标识不改变已安装运行包的身份。

## 已验证

- 基础 B00–B10 的 252 件原始证据及运行包哈希一致，独立复核通过。
- Go 全量测试、7 个布局评分基准、原理图规则正负例和 1028 个块库引脚引用审计通过。
- 已安装 CLI 的合成用例通过原理图分区、求解、排版、渲染及固定页面 Compose，
  PCB 二层 solve/check/render 也通过；12 个正例和 11 个预期拒绝负例经独立复核通过。
- 预检现在按唯一目标工程/文档窗口判定版本和宿主；另一个工程的旧连接器不再误拦目标。
  目标缺失、重复、版本不符或宿主不兼容仍拒绝。9 项回归和目标 PCB 现场预检通过。
- 已有基础测试 PCB 与上轮清理快照仅采集时间不同，没有新增器件或铜。

## 继续条件

`ceshi` 当前是已有 AT32F415 晶振工程，不能作为空白板清理。建议复用已验证的
`ceshi-cli-basic-20260924`，冻结新目标页并补齐该范围的基础检查；也可另建独立工程重跑基础验收。

无历史设计上下文的执行 Agent 已仅从 ESP32 客户原始需求准备方案。推荐叠层为：顶层元件与信号、
内层 GND PLANE、内层 3.3V 电源铺铜、底层 GND 为主；另一选项将 3.3V 也设为 PLANE，
对应两个内电层。确定原需求的解释后，再完成真实库身份/引脚核验及 A01–A06。
PCB 布局仍须两轮回读检查并展示预览，由用户确认后进入整板布线。

| 用例 | 当前状态 |
|---|---|
| A00 需求与选型 | blocked：方案已准备，工程/叠层待确定，真实选型核验未完成 |
| A01–A06 | not-run：未启动高级现场规划、写入或整板验收 |

本轮只保存了原工程文档和读取既有测试夹具，没有修改电路对象、AT32F415 基线或发布资产。
