# 高级 CLI 续测：2026-09-26

最新续测使用 `1.7.1-dev.9`：连接互踢已修复，B00–B03、B05–B07 独立通过；B04 改名
仍失败，B08 又发现区域/铺铜边界宽度未兑现。失败对象已清理，继续核验独立基础项；
当前状态见 [dev.9 逐项报告](2026-09-26-v1.7.1-dev9-basic-cli.md)。
**高级 A00 仍 blocked，A01–A06 未运行**。下文保留最初 dev.21 测量和后续修复经过，
不将历史基础通过项计入 dev.9。

本轮已完成同版基础证据复核、目标工程只读预检和离线回归，修复了多窗口导致的预检误拦。
**高级现场 A00–A06 未通过**。按用户“复用＋A”续测后，删除器件重复出现真实残留，
已停止依赖该基础能力的高级现场用例，测试夹具已清理并保存重载回空白基线。
命令、证据和边界见[详细记录](2026-09-26-advanced-cli-detail.md)。

本轮测量时 CLI、daemon、目标连接器为 `1.6.0-dev.21`，Web EDA 为 `4.1.60`。
不是 `1.7.0` 现场复测；源码工作树的正式版本标识不改变已安装运行包的身份。

## 已验证

- 基础 B00–B10 的 252 件原始证据及运行包哈希一致，独立复核通过。
- Go 全量测试、7 个布局评分基准、原理图规则正负例和 1028 个块库引脚引用审计通过。
- 已安装 CLI 的合成用例通过原理图分区、求解、排版、渲染及固定页面 Compose，
  PCB 二层 solve/check/render 也通过；12 个正例和 11 个预期拒绝负例经独立复核通过。
- 预检现在按唯一目标工程/文档窗口判定版本和宿主；另一个工程的旧连接器不再误拦目标。
  目标缺失、重复、版本不符或宿主不兼容仍拒绝。9 项回归和目标 PCB 现场预检通过。
- 已有基础测试 PCB 与上轮清理快照仅采集时间不同，没有新增器件或铜。
- 复用 P1 的放置、逐脚连接、保存重载补测通过，测试对象已精确清理并重载回空白基线。
- 官方全页 SVG 与原生归档补齐精确纸张边界；新增 `sch sheet-geometry --from-svg` 离线入口，
  保存源哈希及内框/标题栏数据，不再靠比例估计签精确纸张检查。

## 已确认范围

`ceshi` 当前是已有 AT32F415 晶振工程，不能作为空白板清理。用户已确认复用
`ceshi-cli-basic-20260924`，冻结目标页并补齐该范围的基础检查。

无历史设计上下文的执行 Agent 已仅从 ESP32 客户原始需求准备方案。用户已确认叠层 A：
顶层元件与信号、内层 GND PLANE、内层 3.3V 电源铺铜（SIGNAL 类型）、底层 GND 为主。
该决定用于本轮真实库身份/引脚核验，不重复要求确认这些选择。
PCB 布局仍须两轮回读检查并展示预览，由用户确认后进入整板布线。

| 用例 | 当前状态 |
|---|---|
| A00 需求与选型 | blocked：22/35 实例完成测量；U4、C8 删除均出现保存的真实失败，13 个实例未测 |
| A01–A06 | not-run：未启动高级现场规划、写入或整板验收 |

第 20 个实例 U4 删除失败后，typed 同页恢复可精确清理，原序列连续复跑两次通过；
继续测量时 C8 再次失败，说明问题不局限于 ESP32 模组。不能靠逐件重开页面刷过验收。
CLI 已修正相关错误恢复提示；再次清理只恢复夹具，不补签原调用或高级验收。
后续已定位同步事务竞争并完成定向修复：dev.3 共 40 轮、最终 dev.4 共 6 轮及保存重载通过，
详见[删除修复记录](2026-09-26-sch-delete-fix.md)。dev.4 基础复跑的 B00/B01 已通过，B02 目标重连受阻，见[本批基础记录](2026-09-26-v1.7.1-dev4-basic-cli.md)。
重连修复后 dev.7 B00–B03 已独立通过，但 B04 改名失败，见[本批基础记录](2026-09-26-v1.7.1-dev7-basic-cli.md)。
dev.8 已[验证失败传播修复并恢复测试基线](2026-09-26-page-rename-fix.md)，宿主改名仍失败。
同包全部基础门禁通过前继续保留 A00 blocked、A01–A06 not-run；不沿用 dev.21 的基础通过结论。

第一轮准备只保存原工程文档和读取既有测试夹具；后续现场操作独立记录在 `run02/`。
AT32F415 基线与发布资产不属于本轮修改范围。
