# v1.7.1-dev.9 B02 daemon 自动重连

执行与独立复核结论：**pass**。

双页已分别 typed 保存并返回 `saved:true`，随后冻结原理图完整对象及含铜 PCB dump。使用已安装 dev.9 执行一次 `make local-daemon-restart LOCAL_EASYEDA=/usr/local/bin/easyeda LOCAL_VERSION=1.7.1-dev.9`；未构建、安装、刷新网页或进行 GUI 恢复。

- daemon PID `57078 → 2615`；原连接从 12:58:32 UTC 已持续超过 14 分钟。
- 重启命令调用：`2026-09-26T13:13:13.091702+00:00`；唯一新连接注册：`2026-09-26T13:13:23.282659Z`，间隔 **10.190957 秒**。这是命令调用至注册时间，不冒充精确停机时长。
- 新窗口 `97085490-2293-4453-a995-bbfc5f898336`；CLI/daemon/connector 均 dev.9、Web 4.1.60，注册时工程 `0f46d4361c4741a9ab7a9ed62164ca9b`、当前 P1 `e453c0063919726b` 均与重启前相同。首次 ready health 观察时间 `2026-09-26T13:13:34.163098+00:00`，60 秒有界等待内成功；没有出现其他连接。
- 新窗口参数另存 `parameters-live-v2.json`。P1 fresh 完整 result/context 与重启前严格相等；PCB 完整 dump 只差 `capturedAt`，语义 SHA256 `41b23a1b3e7c54f623efa678dea430033717968444fefee90e1ccbfebce0d527`。空板无器件、无外形的 partial 原样保留，不补签几何能力。
- 14 条已完成 CLI 记录均 exit 0；96 条审计均 ok，domain 回包未发现 ok/verified=false。重启调用至首次 ready health 之间 audit action 为 0；之后读回只使用新窗口，最终回到 P1。

前台 daemon 仍在运行（session `36166`），其最终退出码为 **null/未退出**，不记为 exit 0。`capture-restart.py` 透明执行上述 make 命令并持续保留完整 stdout/stderr；本项冻结日志副本供复核。最终完整记录 `09-restart-completed.command.json` 由标准记录器在进程自然退出后生成，不纳入当前冻结 manifest；活动日志也不纳入。没有为了补退出证据再次重启 daemon。

原始证据：[B02 目录](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B02)；冻结清单：[manifest.json](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B02/manifest.json)。本项结束后停止现场调用，B03 未执行。

独立复核报告：`artifacts/cli-basic-dev179-20260926/review/B02-independent-review.md`，
SHA-256 `cd5ebe28afe249438e7871e3dab3a1bd5589738a9d09d68c084bd5c1e3a0f0d3`。
27 件冻结文件和 7 项输入哈希一致，83 个独立断言通过。首次 ready 观察在调用后
21.061866 秒，注册时间仍按上文单独计算。只签本次 daemon 自动重连。
