# v1.7.1-dev.9 B05 库读取与单器件操作

执行与独立复核结论：**pass**。B04 重命名仍为 fail；本项使用独立恢复的原测试工程 P1，不补签 B04 或放行高级。

- 当前真实库 search/by-lcsc/device/symbol/footprint 均完成：C25804、10kΩ、R0603；device `b948db94476e4027ac8953235755ec96`，单子部件。库读取未改库。
- 只 place 一次 R901，primitiveId `577f766eaf8a9e3e`，初始 `(300,400)/0°`。fresh list 有真实两引脚、bbox、全类原生对象；include-device-identity 将本地实例映射到上述真实 device 和 footprint 源。
- 只 modify 一次至 `(400,400)/90°`。fresh 引脚 1 `(400,380)/270°`、引脚 2 `(400,420)/90°`，与参数计算完全一致；位号、身份和全部非几何字段保持，13 个原属性保留。修改前后范围外全类对象严格相同。
- place 前后及最终范围对账只排除这个精确器件、其实际 pin ID 所属属性和派生计数；原有所有图元/属性身份和内容保持。唯一范围外差分是图框 `@Update Time`，旧值 `18:00:46`、最终值 `21:36:01` 单独记录；该值在 modify 及后续保存期间更新，不能解释为保存发生的精确秒数，不忽略其他字段。
- 显式 save 返回 saved:true，随后完整 fresh 目标器件与修改后全等。当前原工程 `0f46d4361c4741a9ab7a9ed62164ca9b` / P1 `e453c0063919726b`，唯一 window `97085490-2293-4453-a995-bbfc5f898336`、daemon PID 2615；CLI/daemon/connector 仍 dev.9。未在本项冒充完成 reload 持久化或设计连通验收。

21 条记录命令（含 5 条 --help）均 exit 0；34 条 audit 均 ok，未出现 domain ok/verified=false。两处离线 runner 假设错误已保留并按真实模型纠正：默认 list 的 device.uuid 是实例 ID；引脚属性归属要包含真实 pin ID。另一次严格范围断言发现的差异仅为已核实的自动保存时间。没有重试 place/modify，也没有 GUI/debug 工程写入。

`14-after-place` 同次回包的 components.uniqueId 仍为空，原生 pagePrimitives 中目标组件已为
gge1；15/16 后两者稳定为 gge1。这是目标器件生成字段的实际暂态，不宣称放置起所有字段
始终相同。modify 的 16→18 对照及保存后的目标非几何字段严格一致。

唯一临时器件 R901 已保存并保留供 B06；[后续输入](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B05/next-B06-inputs.json)绑定完整 baseline `20-after-save.json` 及 SHA。B06 未执行，B04 保持失败，基础门禁仍未通过。

原始证据：[B05](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B05)；[最终对账](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B05/final-verification.json)；[冻结清单](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B05/manifest.json)。

独立复核：`artifacts/cli-basic-dev179-20260926/review/B05-independent-review.md`，
SHA-256 `f4b07b896df2f8987b6a7eed890f7b4b66fbb6d15c031bc8bb04332883740e0c`；
最终 inputs-v2 SHA-256 `943bec3fcfd8b80d024eb86deaf96ffb6f61e5d7e0aa7e03ef42b33c9f428a56`。
36 件冻结文件、库到实例映射、引脚/bbox 变换、13 属性和 37 个原生属性身份闭合。
审计冻结文件按文本时间排序，与原始追加行序不同；复核改按解析 UTC 与 requestId 对齐后
确认全部 34 条原记录一致、无增删。初次行序断言的未闭合记录保留，不改原证据。
