# 未关闭 Issue 分类与版本核对（2026-09-12）

核对基线：GitHub latest `v1.4.8`（tag `8c98ef7`）；远端 main `fe09502`；
本轮开始时本地 main `322b5c0`，安装态 CLI/Skill/daemon/Connector 为 `1.4.9-dev.1`。
三者不可混称“当前版本”。以下“仍存在”指缺陷代码路径仍存在，不表示本日真机复现，
也不把历史日志中的发生次数/失败率套到新版。

| Issue | 归类 | v1.4.8 / 开发源码核对 | 本轮处理 |
|---|---|---|---|
| #204 | 自有代码 Bug：库 build 缺少已有几何保护 | 两版均直接追加 pad/pin；symbol 也受影响 | 保留；优先补非空拒绝及作用域验证 |
| #205 | 自有代码 Bug：布局连接预算漏配 | 两版布局公共出口均继承 20s，connect/autoconnect 用 35s | 公共出口已统一 35s；离线测试已覆盖实际请求及页面守卫；未发布 |
| #206 | 自有代码 Bug：状态未知后盲目重试 | 两版 zaaRetry 和 move 内核均存在 | 保留；不能直接复用仅凭 rolled back 文案的判据，需与 #208 联合设计 |
| #207 | 导航超时/落地判定缺口 | 两版 document.open 均用默认预算；历史窗口停摆与预算因素需区分 | 保留；需要当前宿主逐步耗时和目标页面回读 |
| #208 | 连接器超时后晚到写入风险 | 两版超时不取消宿主 Promise，回滚缺少迟到结果处理 | 已订正标题；整条请求耗时不能用于单步预算定标 |
| #209 | 自有代码 Bug：队列健康诊断宣称未执行的旁路证据 | 两版原来均只有入队探针 | 已加入并发旁路读、短超时/证据有效期和未知状态拒绝；未发布 |
| #211 | 自有代码 Bug：脚本编译拒绝染健康度 | 两版原来编译/执行共用 catch | 已拆开；编译拒绝不执行有效前缀，执行期 SyntaxError 仍计真实失败；未发布 |
| #212 | 文本搬移失败后的部分完成/恢复缺口 | 两版均先移动器件再搬文本，文本失败直接退出 | 保留；undefined 不证明零变异，须回读去重后补偿；3–10s 重试建议未验证 |
| #213 | 超时预算语义与错误传播不一致 | 两版 place=8s、daemon 等待=6s；结构化失败被转换为普通失败 | 保留；“永不触发”仅适用于结构化 daemon 超时路径，裸 HTTP 超时仍可触发提示 |
| #214 | 重读预算覆盖不一致 | 两版 includePins 仍有默认20s/显式90s；新 identity 60s 仅针对 includeDeviceIdentity | 保留；预读失败可回退重读，不等于整个 gate 假绿；150s 尚未验证 |
| #191 | 宿主 API 兼容性 + 本仓库降级/超时处理 | v1.4.4 已绕开无关旋转校准；宿主 createNetLabel 不完成的报告未获新版实测解除 | 保留；移除错误的 block-bug 分类，标上游/需现场验证 |
| #173 | 新功能：PCB 持久化编组 | 原生组接口未暴露；虚拟组需求未实现 | 保留 feat；不属于现有功能回归 |
| #43 | 芯片级整板端到端验收任务 | 目标未完成；前置依赖解除不等于自身通过 | 独立验收分类；不当作单个 Bug 修复 |

#210 已关闭：原归因不成立、所指自动化调用路径已于 8ecdaf5 移除。

## 首批修复的验证和交付边界

- #205：HTTP 测试确认布局 connect_pin 实际发送 35000ms、普通修改保持 20000ms，错误页面仍拒绝。
- #209：旁路必须真实返回 ok，失败/无结果/过期不能证明连接器健康；旧探针结果不能污染新探针。
  `CONNECTOR_HEALTH_UNVERIFIED` 不进入 CLI 的自动队列等待；诊断读本身只读，不改变 EDA 图面。
- #211：编译错误前的有效写代码不得执行；运行时抛 SyntaxError 仍保留 EDA_CALL_FAILED。
  现有 daemon 请求拒绝豁免保持不变，不扩大健康度豁免范围。
- Skill 已同步错误处理与预算说明。只有安装这些改动后的运行时才具有新行为。
- 自动验证已通过：`go test ./...`、daemon 队列相关 race 测试、连接器全部 284 项测试、
  TypeScript typecheck、`make skill-check` 和 `git diff --check`。
- 当前正式版门禁未通过（latest 为1.4.8，本机为开发版）。未降级/替换安装态，未操作现场，
  未运行要求的 ceshi 全流程 E2E。保持 Issue 打开，不能把离线通过写成已发版/真机验收完成。

后续顺序：完成首批现场回归 → #206/#208 的可验证重试与晚到处理 → #204 库几何保护
→ #207/#213/#214 的分层预算与错误传播 → #212 文本补偿；#191 需宿主验证，#173/#43 独立排期。
