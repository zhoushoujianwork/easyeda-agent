# v1.7.1-dev.9 B03 个人工程容器

执行与独立复核结论：**pass**。本批真实运行 CLI/daemon/目标 connector 均为 dev.9；Web 4.1.60。独立复核已完成。

- 个人空间新容器 `ceshi-cli-basic-dev179-20260926` / `9d80866defa649a48bbd7a808676160d`；create 仅 1 次且省略 --team。创建前完整枚举 55/55，根目录同名 absent；创建后 56/56，唯一同名 UUID 与 info 一致。
- 默认 P1 `9fabe83f7425eb88`、原理图父文档 `6b2fd3a4beefcacb`、PCB `4129bcfbf0fb3081` 已 fresh 核对；显式 project open 返回 verified/documentVerified，实际页回读匹配。
- 原工程和新工程各两页均显式保存并返回 saved:true。原生导出 11646 字节，SHA256 `b25b0ee01fd957689a21494f599d752496a7c6e47b758ee6b8d3272ad398e72c` 与回包一致，全部 ZIP CRC 通过；project2.json 标题及宿主版本正确，未验证重新导入。
- 返回原工程 `0f46d4361c4741a9ab7a9ed62164ca9b` / P1 `e453c0063919726b`；P1 完整 result/context 不变，PCB 完整 dump 与 B02 冻结结果仅 capturedAt 不同。最终唯一 window `97085490-2293-4453-a995-bbfc5f898336`、daemon PID 2615，三方仍 dev.9。
- 30 条 CLI 退出码均为 0；121 条 audit 中 2 条 document.current 为 No active document，保留为内部 action 失败，不能说全部 action 成功。没有 domain ok/verified=false。

创建后约 0.39 秒的首次 `doc ls` 返回 `activeUuid:"", documents:null`，该次不计文档列表通过。依照工程身份先于文档树就绪的已知行为，只进行一次完整同名 find 和一次 fresh docs；后者取得默认双页，未重复 create、未刷新或 GUI 恢复。原始 null 回包及 runner TypeError 均保留。

`project open` 的汇总 envelope 不带 context；一次额外 context 断言属于 runner 假设错误，原命令 exit 0、result 的 uuid/documentUuid/verified/documentVerified 正确。未重跑 open，以后续 typed pages/list 独立核对实际上下文。见 open-envelope-assertion-note.json。

新空容器保留供 B04–B10；没有 typed 工程删除命令，不承诺删除容器。未触及其他非测试工程。B04 未执行。

原始证据：[目录](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B03)；命令 UTC、退出码和完整 stdout/stderr 见各 JSON。

独立复核报告：`artifacts/cli-basic-dev179-20260926/review/B03-independent-review.md`，
SHA-256 `1b63d9003a0faeaf12663d748635e76708ae8a879f209da20a5953788083a000`。
43 件冻结文件、146 个断言闭合。121 条审计实际为 119 成功、2 次已处理读取失败。
后续 find 不代表活动页面恢复；fresh inventory 就绪后的一次显式 open 及随后回读闭合该判据。
