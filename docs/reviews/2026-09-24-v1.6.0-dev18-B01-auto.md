# B01 单项报告：命令契约、Skill 签名与零调度审计

## 结论

`pass`（本表定义的基础命令范围）：帮助、关键签名、错误语法和公开 Skill 中对应 flag 引用均通过；检查期间审计不变。

## 前置条件与范围

- 2026-09-24，Web EasyEDA Pro 4.1.60；CLI/daemon/connector 标签 dev.18，CLI 含 `c6bdc05` 查找预算补丁。
- 本轮从源码 `402419c` 开始，后续提交仅更新测试工具与文档，没有更换运行时。
- 工程 `ceshi-cli-basic-20260924`，UUID `0f46d4361c4741a9ab7a9ed62164ca9b`；窗口 `b0e82c52-5c4a-44c1-ae80-f13a37d9cd1b`。
- 临时原理图页 `45355d82797ca512`；默认 PCB `3037fc1dfa4965d2`。
- 本项只有基础动作及直接回读；没有运行规划、求解、Compose/Apply 或 DRC 业务验收。

## 检查与实际结果

- `scripts/cli-basic-contract-check.py` 已从 49 个入口扩展到 75 个，补齐命令域、库 device/symbol/footprint、页面删除、配置/层数子命令及几何精确清理入口；`project find --timeout` 纳入关键签名。
- `contracts-complete.json`：75 个帮助均退出 0，160 项 typed action 目录可解析，其中本表 6 个关键动作存在；3 个错误命令/flag 均非零退出。
- 扫描 `.agents/skills/easyeda-agent/**/*.md`，104 处本表基础命令引用的 flag 与实际帮助对应。每处记录文件、行号、片段和缺失参数列表，本轮缺失为 0。最终产物另保存 75 条帮助的 stdout/stderr、安装 CLI/checker SHA-256，以及 action 目录原文，可离线复核实际帮助。
- 检查前后 58 个 JSONL 文件的 SHA-256 完全相同，证明这轮离线检查没有追加 EDA 调用记录；测试时无并发现场操作。
- 另用临时 Skill 放入不存在的 `--not-a-real-basic-flag`，检查器明确非零退出且审计不变；没有执行示例中的设计写入。
- 边界：这是命令名/flag 名存在性检查；不证明示例参数值或设计语义正确，不覆盖目录外高级 CLI，也不把 160 项目录存在当作 160 项现场通过。

## 原始证据

本机目录 `artifacts/cli-basic-auto-dev18-20260924/B01/`。每份命令 JSON 保留完整参数、
UTC、退出码、stdout/stderr；`evidence-sha256.json` 保存本项 JSON 哈希。以下时间为 UTC。

| 文件 | 开始 | 秒 | 退出码 |
|---|---|---:|---:|


## 清理与边界

此项只读本地帮助、Skill 与审计，无 EDA 写入或清理。现场动作结论仍按其他 Bxx 单项。
