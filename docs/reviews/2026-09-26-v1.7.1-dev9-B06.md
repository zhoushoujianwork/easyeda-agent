# v1.7.1-dev.9 B06 原理图连接原语

执行与独立复核结论：**pass**。B04 保持 fail，本项独立验证不放行整批门禁或高级。

以 B05 保存的 R901 全量数据为基线，当前 fresh result/context 起点严格相同。全部动作显式绑定原工程 `0f46d4361c4741a9ab7a9ed62164ca9b` / P1 `e453c0063919726b`、window `97085490-2293-4453-a995-bbfc5f898336`，三方 dev.9 / Web4.1.60。

- 一次创建真实导线 `3bebc45d8763fa76`：实测 pin2 `(400,420)` 到 `(400,450)`，长度 30 raw；宿主回读端点顺序反向，端点集合和几何相同。导线连接引脚后，pin2 获得宿主匿名网 `$1N4`。
- 一次创建 netflag `8754f7bc03bdd307` 于导线远端，网络 `BASIC_DEV179_NET`。fresh pin2 网络精确匹配，存在非零真实 wire；未把标志直接叠在引脚上充当连接。
- 对未接线 pin1 一次设置 NC，fresh noConnected:true；一次 clear 返回 notApplied:[]、exit0，fresh false。stderr 的 **bug #257 警告原样保留**，本次成功不能关闭历史间歇问题，没有自动重试。
- 每步完整 native 对象、引脚、导线、bbox 回读并比较范围。只允许明确新 wire/flag及其属性、相应 pin2 网络或 pin1 NC 变化；其他图元/属性保持。中间第一次范围断言未允许 wire 导致的 pin2 匿名网变化，实际差异原样保留于 wire-create-full-differences.json；随后按该明确连接语义核对，不重放写入。
- flag 和 wire 各精确删除一次，fresh 确认消失。最终仅保留 B05 R901，两脚 net 为空且 NC=false。显式 save → reload → fresh 后仍成立。

重载后完整 components（含 bbox、引脚几何和电气状态）及所有非属性结果严格相同。69 个 native 属性的 parent+key 唯一对应，其中 **9 个宿主属性 ID 重铸**：3D Model/Title/Transform 及两引脚 Name/Number/Type。每项父对象、键、值、可见性、坐标、旋转、字体等所有其他字段逐一严格相同，其余 60 项 ID/字段不变；映射完整保留，未通过整体删除 ID 字段放行。3D 属性可见性原样 null、引脚属性 false，未声称做过图像对照。最终与初始基线仅差这些明确映射及图框自动日期/时间字段；时间记录是宿主元数据观察，不精确归因到某个 save 秒。

19 条 CLI 均 exit0；67 条 audit 全部 ok:true，本项未出现 No active document 失败。所有 domain 回包无 ok/verified=false。原始审计切片保留追加行序，与原始日志逐条相同。

[后续状态](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06/next-B09-inputs.json)绑定保存重载后的完整 baseline 与 SHA：R901 `577f766eaf8a9e3e` 保留，所有本项 wire/flag/NC 已清理。B07–B10 未执行。现场交回 root，未改源码、包或 daemon。

原始证据：[B06](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06)；[逐属性映射](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06/reload-attribute-identity-map.json)；[最终对账](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06/final-verification.json)；[冻结清单](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06/manifest.json)。

范围补充：B06 的属性映射不补签 B09 渲染/电气效果判据；B09 仍需自己的前后 typed 导出/网表或能证明效果的完整数据。冻结目录保留原报告中的审计模板误句，本页已据原始 67 条全成功记录修正。补充见 [boundary-addendum.json](/Users/mikas/github/easyeda-agent/artifacts/cli-basic-dev179-20260926/B06/boundary-addendum.json)。

独立复核：`artifacts/cli-basic-dev179-20260926/review/B06-independent-review.md`，
SHA-256 `d9dd5439c65c43c1c03ec3b55d7cb9f7d8d5a4cc24ae250eb663885438e0ae2b`；
inputs SHA-256 `08c9a60109ba6ee2f145dee0bd75366723bc653bf5835bb7aab1ae60323deb92`。
39 件原清单及补充清单匹配；单次图元生命周期、NC 清除、69 个属性逐项映射和
全部非属性结果闭合。后续输入完整 baseline 哈希、R901 身份与引脚均有效。
