# dev.11 铺铜回读定向现场验收

结论：**错误检测与退出传播已现场验证；本次定向计划未全部通过。** 显式宽度1 mil仍读回0.2 mil，CLI正确失败；预声明的默认宽度用例又遇到优先级2读回1，未取得verified:true正向路径。两用例均未重试，全部临时对象已精确清理、保存重载并恢复原空白基线。

## 版本与安装

源码提交 `07c3060` 的17个文件SHA与候选v3清单逐一相等。安装包为 `dist/local-v1.7.1-dev.11`；CLI SHA256 `cfd84af174019cb31404f1b32d89e831184bd12660599837859194f79e7029fd`；bundle SHA256 `035565f83ad0719426e03cb4d1b3331b5c45b11f2ec63eb6457463cad3366faf`。没有安装dev.10或创建发布。

旧dev.9唯一窗口97085490…、PID2615中，P1完整result与B10最终全等，PCB仅capturedAt变化，双页均saved:true。fresh读取同UUID连接器存储确认旧bundle71b13d1e…、版本与权限；通过已提交hot-reload-server/inject有界更新同一UUID，两个存储记录原子写入、回读哈希及权限保持，脚本安排reload。只管理连接器，没有JS工程写或GUI操作。

新daemon PID19250，唯一窗口 `be65ae0d-4680-41f1-b94d-681fd7eb653b`，connectedAt `2026-09-26T15:11:31.832215Z`。CLI/daemon/connector精确dev.11、Web4.1.60，原工程 `0f46d4361c4741a9ab7a9ed62164ca9b`、P1 `e453c0063919726b`、PCB `3037fc1dfa4965d2`。23本地安装核对ready:true；切PCB后29 health仍同版唯一。升级前后SCH完整全等，PCB仅capturedAt不同，原配置和260层全等。

## 冻结参数与实际结果

`test-parameters.json` 在第一次工程测试写入之前同时声明两块不同polygon。单位mil、y-up；fixture原点(1000,1000)，矩形400×300，第二块偏移(600,0)。独立测试网DEV11_POUR_READBACK由一条typed TOP导线(1050,1100)→(1350,1100)、宽10建立。当前无pad/器件/板框，不宣称任何pad连通、设计布线或DRC通过。

| 用例 | 请求与实际 | 判定 |
|---|---|---|
| ①显式宽度 | width1、priority1、name DEV11_EXPLICIT_WIDTH。fresh width0.2，其余网/层/填充/名称/优先级/边界几何兑现；ID21ae77c2f6e5df58 | CLI exit1，verified:false/partial:true、differences=[lineWidth]、rebuildAttempted:false。检测修复通过，宽度请求未兑现。 |
| ②宿主默认宽度 | 事先独立声明不传width，priority2、name DEV11_HOST_DEFAULT。fresh width0.2、priority1，其余声明字段兑现；IDd4d31836a049675d | CLI exit1，verified:false/partial:true、differences=[priority]、rebuildAttempted:false。此用例fail，正向verified:true未覆盖。未改priority或补宽度重试。 |

两次create均为外层ok:true，CLI正确根据domain失败退出，并保留实际字段与ID；不能把audit外层成功当设计写入兑现。fresh列表与全dump逐字段绑定，边界顶点仅顺逆序规范化，完整边集合精确相同。其他对象不变。

两次poured均false。①poured-list实际available:true/count1，有一份fill精确归属于该pour ID；即使连接器没有调用rebuild，宿主仍已产生材料化铜。②available:true/count0。**rebuildAttempted只描述连接器调用，poured、实际材料化铜分别是不同事实**；没有额外rebuild或试探。首次范围断言漏计①派生fill而失败，停在清理前；保留原dump及修正说明，在确认fill父ID后才清理，没有放宽范围至其他对象。

优先级2的来源是本项预声明第二独立用例参数，不是现场失败后调整。31 --help支持priority整数；官方本地SDK `@jlceda/pro-api-types` 的create签名第7参数是pourPriority、第8是lineWidth，源码传参顺序相同。签名和全文件SHA保存在official-signature-source与原始摘录中；它不解释宿主为什么读回1。原因未确定，可能与宿主排序语义有关，不能只据一个对象称宿主bug。

## 清理、证据与限制

①删除前fresh确认exact ID及派生fill，删除后全dump严格恢复单track基线；②同样精确删除并完整恢复track基线。最后fresh删除唯一track `73e6132ea33676b3`，仅移除本批对象。60 PCB saved:true → 61 reload → 62 full dump，63配置/64层均与原值完全一致；netRules=[]，260层全等，PCB semanticSha256 `41b23a1b3e7c54f623efa678dea430033717968444fefee90e1ccbfebce0d527`，完整PCB对B10仅capturedAt变化。原空板partial保留，不补签板尺寸几何。

P1范围未改，67 saved:true → 68 reload → 69 fresh完整result与B10严格全等（连派生时间也未变）。70最后health后现场明确交回root。daemon仍运行，生命周期exit为null，冻结的是过程/日志snapshot，活跃日志原路径从manifest排除，不伪造退出。B03容器仍保留，其他工程未访问。

共70个easyeda CLI命令（包含离线帮助/成本）、另一个daemon启动过程及一个WS服务器过程；71个已结束进程记录中67 exit0、4 exit1：13安装误加仅check可用的--json，写前拒绝后14正确安装；34查询误用不存在的tracks，本地拒绝后full dump与help确认使用track-list；40/48是两次真实domain失败。前两项audit零调度，错误记录保留，均未重放工程创建。

冻结audit共368条、外层全ok，包含三次仅连接器管理的debug调用和两次domain失败；原追加顺序保留。命令窗口UTC为15:08:41.030127–15:17:13.906514，共512.876387秒。72仅离线audit cost追加台账一次，未传tokens；其实际audit事件墙钟8.180313分钟、daemon9.739秒、差值481.079802秒，tokens字段默认0代表未记录，不是实测零消耗。

`verify-evidence.py --check` 重算105项证据断言；断言成立不等于两设计用例通过。完整命令、UTC、退出码、stdout/stderr、参数、安装SHA、官方签名、所有全量状态及失败记录均列入manifest。

**dev.11完整B00–B10未运行；dev.9 B04/B08失败不补签；A00仍blocked，A01–A06 not-run。** 本次只证明有限错误回读/传播和安全恢复，正向成功分支仍有现场覆盖缺口。

[冻结证据目录](/Users/mikas/github/easyeda-agent/artifacts/cli-pour-readback-fix-20260926/live-dev11)。

## 独立复核

400 项独立证据检查通过，结论为：检测与失败退出 pass；默认宽度计划因优先级不符 fail；
`verified:true` 现场未覆盖；精确清理、保存重载及完整基线恢复 pass。检查数不是设计用例通过数。

补充上述材料化铜的证据边界：43/50 原始库存均带 `staleRisk`，失败铜未保存重载。
①有 exact-parent fill、②列表为空仅是当时库存观察，不能据此验收铜的持久化。
清理后的完整基线另经 save → reload → fresh 核对，且直接与 B10 原配置和全部 260 层比较一致。
独立检查器首次漏掉 track-delete 由 CLI 注入的 `kind:track`，原失败保留；按 `07c3060`
实际命令映射精确修正为 v2 后通过，没有修改现场原始记录或重放动作。

独立报告 `artifacts/cli-pour-readback-fix-20260926/review/dev11-live-independent-review.md`，
SHA-256 `478d2db894cecc96ffee8a8b63e1b1a5e655e6024a35b70a842e5cb730856e4a`；
原始输入与 394 项检查清单 v2 SHA-256
`4b80a8bf5c975a42926319790a08ab94f29761390bc5cc674ee0ba9378495833`；
6 项补核清单 SHA-256 `a1b50ce47a9c17360ca648dbe6bc597874df1c1716dc63c5c9fd514f9b9f1f85`。
112 件现场 manifest SHA-256 `30a5f0a2aaadb2aa5b93751f6d8cf5708028309c1e0c8a5e2838e2b636f4b01e`。
本节在复核冻结后追加；原执行报告及其 SHA 保留在冻结证据中。
