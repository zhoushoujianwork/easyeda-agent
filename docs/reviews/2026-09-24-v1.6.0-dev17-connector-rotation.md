# 2026-09-24 Codex 自动轮换连接器与 Web 刷新复测

## 结论

Codex 已在用户原有的内置浏览器 Web EDA 标签中，自动完成同 UUID 侧载连接器的
卸载、导入和“允许外部交互”恢复。最终运行版本为 `v1.6.0-dev.17`，CLI、daemon、
connector 精确同版；typed `easyeda web reload` **通过**，整条命令墙钟 **12.311 秒**。
本轮只复测了两个已知基础故障，它们仍失败；**基础 CLI B00–B10 门禁未通过**，
未执行高级 CLI 验收。

## 开始状态与自动轮换

- 宿主：Web EasyEDA Pro `4.1.60`；工程 `4037dd22434c412fa75d7f2d855a2ab4`，
  目标原理图 P3 `a6db7873157483f0`，已打开 P1/P2/PCB1/P3。
- Codex 先通过带 `--project`、`--doc` 的 typed CLI 分别保存 P1、P2、PCB1、P3，
  四条命令均 `saved:true`；不通过 GUI 保存电路。
- 在当前 Web EDA **高级 → 扩展管理器 → 已安装**，打开目标卡片，核对版本和
  UUID `4dae27407c1d43be98e8e210d45fe587`；点“卸载”，在确认框复核目标并确认；
  点“导入”，经浏览器 `filechooser.setFiles` 选择本地 `.eext`；接受安全提示；打开
  **配置**，将“允许外部交互”从关闭设为开启，UI toast 和复选框均确认成功。
- `dev.15 → dev.16` 后 `health` 已看到三端精确同版，但正式刷新立即失败：
  `web-reload.json` 记录 0.036 秒、`Function is not a constructor`。此失败促成
  `dev.17` 改用项目已有的固定 `AsyncFunction` 宿主全局访问方式。
- `dev.16 → dev.17` 按相同步骤轮换。扩展详情显示 `V1.6.0-dev.17`；本地包
  `extension/build/dist/easyeda-agent-connector_v1.6.0-dev.17.eext` 的 SHA-256 为
  `ab452abe4a32f9ccff737189ebe96c1248b78de5c5845c3b0351d462417326d2`。
  再次启用外部交互后，`health` 显示 CLI、daemon、运行中 connector 均为 `dev.17`，
  目标工程和 P3 身份正确。

浏览器 UI 自动化只用于扩展管理。点击后每步重新读界面状态，未以固定可访问性索引当
长期定位符；复选框点击曾返回工具错误，但状态已变成勾选，因此核对状态后没有重复点击。
导入时必须先监听 `filechooser`，再点击导入并设置绝对文件路径。相同名称的卡片在卸载
旧版、导入新版后会再次出现，须查看版本而非只看名称。

## 正式刷新与短测

原始命令、UTC 时间、stdout/stderr、退出码和墙钟耗时在本机忽略目录
`artifacts/cli-web-reload-dev16-20260924/`、
`artifacts/cli-web-reload-dev17-20260924/`；该目录不是发布验收附件。

| 检查 | 现场结果 |
|---|---|
| `web reload --project ... --doc P3 --timeout 30s` | `dev.17/web-reload.json`：`saved:true`、`reloaded:true`、`ready:true`；旧 window `532696cc-cac2-4dcb-b1a2-b8523998a2ab`，新 window `cb6c89d0-9e47-432f-a004-1d66816d9840`，同一工程/P3。总计 12.311 秒，CLI 回包 `elapsedMs:12296`、`reconnectMs:12280`、`saveMs:3`。 |
| P3 回读 | `dev17/P3-after.json`：13 个器件、15 条导线段；相对 `dev16/P3-before.json`，器件与导线内容相同，267 个属性剔除 `primitiveId`/`ParentPrimitiveId` 后的内容多重集相同；118 个属性 ID 改变，因此不称完整对象快照相同。 |
| B03 `project find` | `dev17/project-find.json`：18.024 秒后 `DISPATCH_FAILED: connector did not respond`，未解决。 |
| B06 `net_label` | 在新建临时页 `928b67036bfad91f` 写入一条 `CLI_TEST_FLAG` 导线后，`dev17/scratch-net-label.json` 在 0.020 秒返回 `EDA_CALL_FAILED`；`scratch-list.json` 新鲜回读没有目标标签。 |
| 清理 | `scratch-delete.json` 删除临时页；`pages-after.json` 只剩 P1/P2/P3；P3 typed 保存成功。 |

`dev.17` 离线 typecheck、518 个 connector 测试、Skill 检查和本地打包检查通过。
这些检查和这次短测不补签 [dev.14 基础门禁缺口](2026-09-24-v1.6.0-dev14-basic-cli-live.md)。
