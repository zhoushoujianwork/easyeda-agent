# 页面改名失败传播修复

**改名失败误报成功已修复，并通过 dev.8 现场对照；宿主改名本身仍失败。**

[B04 现场失败](2026-09-26-v1.7.1-dev7-B04.md)中，宿主明确返回 false，页面保存重载后
仍为 P2。旧连接器却提示“已提交、缓存未同步”，CLI exit 0，让调用方继续执行。

修复只调用一次官方改名接口，并保留宿主返回值与 fresh 页名。宿主拒绝、读取超时、
页名未匹配或页面身份缺失都返回 typed error；CLI 也拒绝旧连接器的内部失败回包。
不会触发无关写入或自动重试改名。Skill 已同步这一边界。

dev.8 单次现场调用正确退出 1，保留宿主 false 与实际页名 P2；独立复核通过。
连接器全量 611 项、Go 全量、类型检查和 Skill 检查通过。

临时 P2 已通过 typed API 删除并保存重载确认，原测试工程的原理图和 PCB 基线已恢复，
独立清理复核通过。新建的个人测试容器保留默认页面。

宿主拒绝改名的根因尚未查明，B04 仍失败；dev.8 未通过同版 B00–B10 全集，
高级 A00 保持 blocked、A01–A06 not-run。

实现、证据与对照结果见[详细记录](2026-09-26-page-rename-fix-detail.md)。
