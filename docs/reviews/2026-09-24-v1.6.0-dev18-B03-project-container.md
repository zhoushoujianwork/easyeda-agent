# B03 单项报告：工程容器查找

## 结论

`fail`：`project find --team` 在 18.025 秒后由 daemon 返回
`DISPATCH_FAILED: connector did not respond`。当前工程信息可读，连接器之后也保持在线；
目前只能确认**查找调用失败**，不能确认是官方枚举、逐项详情、还是我们串行扫描导致超时。
本项的 create/open 因查找身份不可靠而未执行，不能据此断言它们有故障。

## 前置条件

- 执行源码 commit `c476f6d`，CLI/daemon/连接器均为 `v1.6.0-dev.18`；Web EasyEDA Pro `4.1.60`。
- 唯一目标窗口 `ad9f4619-e92f-4e19-90c7-ca951965a701`；已打开工程
  `4037dd22434c412fa75d7f2d855a2ab4`，P3 页 `a6db7873157483f0`。
- 当前工程的 `project info` 确认友好名称
  `easyeda-v160-schematic-clean-20260924` 和 team UUID
  `f60b2174579745258ada9b72bbe2f52b`。

## 步骤与原始结果

原始记录在本机忽略目录 `artifacts/cli-basic-dev17-continue-20260924/`；每份 JSON
保留命令、UTC、耗时、退出码、stdout/stderr。本项全为只读调用，无写前后对象差分要求。

| 步骤 | 原始文件 | 结果 |
|---|---|---|
| 不带团队查当前工程名称 | `B03-find-unscoped-dev18.json` | 2026-09-24 14:08:22 UTC；0.027 秒、退出 0；官方 UUID 清单为空，`presence:unknown`，**没有证明工程不存在**。 |
| 带当前工程 team UUID 精确查找 | `B03-find-team-dev18.json` | 2026-09-24 14:08:33 UTC；18.025 秒、退出 1；`DISPATCH_FAILED` / `context deadline exceeded`，无匹配结果。 |
| 查当前工程信息 | `B03-project-info-dev18.json` | 2026-09-24 14:09:05 UTC；0.023 秒、退出 0；工程 UUID、名称、team 与目标一致。 |

预期是带团队查找返回当前工程的精确 UUID；实际超时，故本项不能通过。
[官方接口](https://prodocs.lceda.cn/cn/api/reference/pro-api.dmt_project.getallprojectsuuid.html)
支持按 team UUID 获取工程 UUID，但其公开签名不证明本次超时发生在该调用内部。

## 归因与清理

**归因未知。** 当前连接器的 `projectFind` 先调用 `getAllProjectsUuid(teamUuid)`，
再按 UUID **串行**调用 `getProjectInfo`，未记录分段耗时或进度。当前证据不足以归咎上游；
串行实现与 daemon 的 18 秒预算不匹配也可能是原因。不带团队的空清单是官方回包，
连接器已正确将其标为 `unknown`，没有当作“不存在”。

未创建、打开或删除工程；当前工程和 P3 已恢复并保持可读，没有本项留下的工程变更。
下一次 B03 只做一件事：在 typed 查找链路记录枚举和每批详情耗时/数量，给官方调用
与连接器扫描分别设置有界预算，再用同一精确名称重测。定位前不盲重试 `project create`。
