# B03 单项报告：工程容器查找

## 结论

`partial`：**团队范围查找子项已修复并通过两次真实调用**。用户要求 reload 和增加
超时后，typed 页面重载 7.703 秒成功；查找完整读取 52 个工程，分别用时
19.382 秒、19.629 秒，均精确找到目标 UUID，`enumeration.complete:true`。

旧 CLI 的 20 秒预算被 daemon 减去 2 秒响应余量，实际只等 18 秒，短于本次正常
扫描耗时。已把 `project find` 默认预算改为 90 秒，并支持 `--timeout 5s..10m`。
这解决了已复现的查找超时；未做分段计时，不能进一步断言具体哪个官方子调用最慢，
也不能从“刷新后成功”推断刷新是必要条件。create/open/export 尚未续测，B03 整项不补签通过。

## 前置条件

- 执行源码 commit `c476f6d`，CLI/daemon/连接器均为 `v1.6.0-dev.18`；Web EasyEDA Pro `4.1.60`。
- 唯一目标窗口 `ad9f4619-e92f-4e19-90c7-ca951965a701`；已打开工程
  `4037dd22434c412fa75d7f2d855a2ab4`，P3 页 `a6db7873157483f0`。
- 当前工程的 `project info` 确认友好名称
  `easyeda-v160-schematic-clean-20260924` 和 team UUID
  `f60b2174579745258ada9b72bbe2f52b`。

## 初测步骤与原始结果（修复前）

原始记录在本机忽略目录 `artifacts/cli-basic-dev17-continue-20260924/`；每份 JSON
保留命令、UTC、耗时、退出码、stdout/stderr。本项全为只读调用，无写前后对象差分要求。

| 步骤 | 原始文件 | 结果 |
|---|---|---|
| 不带团队查当前工程名称 | `B03-find-unscoped-dev18.json` | 2026-09-24 14:08:22 UTC；0.027 秒、退出 0；官方 UUID 清单为空，`presence:unknown`，**没有证明工程不存在**。 |
| 带当前工程 team UUID 精确查找 | `B03-find-team-dev18.json` | 2026-09-24 14:08:33 UTC；18.025 秒、退出 1；`DISPATCH_FAILED` / `context deadline exceeded`，无匹配结果。 |
| 查当前工程信息 | `B03-project-info-dev18.json` | 2026-09-24 14:09:05 UTC；0.023 秒、退出 0；工程 UUID、名称、team 与目标一致。 |

预期是带团队查找返回当前工程的精确 UUID；初测实际超时，故当时判失败。
[官方接口](https://prodocs.lceda.cn/cn/api/reference/pro-api.dmt_project.getallprojectsuuid.html)
支持按 team UUID 获取工程 UUID，但其公开签名不证明本次超时发生在该调用内部。

## reload 与延长预算复测

基于 `d1d01db` 加本报告同提交的 CLI 补丁；CLI 构建标签仍为 `v1.6.0-dev.18`，
daemon PID 46999 和连接器未更换，均为 dev.18；这是 CLI 超时修复的单项验证，
不代表原 dev.18 发布包或 B00–B10 全量通过。新二进制已安装到 `/usr/local/bin/easyeda`。

重载前逐页 typed 保存 P1 `e411581b51077d9c`、P2 `8844d02959ede391`、PCB1
`3f0eef6cc0981d19`、P3 `a6db7873157483f0`，均返回 `saved:true`。
新窗口为 `b0e82c52-5c4a-44c1-ae80-f13a37d9cd1b`，工程/P3 与前述目标一致。
原始记录在 `artifacts/cli-basic-dev18-B03-retry-20260924/`，每份 JSON 含完整命令和回包。

| 步骤 | UTC 开始 | 耗时 / 退出码 | 证据与结果 |
|---|---|---|---|
| `web reload --project <目标UUID> --doc <P3> --timeout 45s` | 14:35:04 | 7.703 秒 / 0 | `reload-correct-project.json`；`saved/reloaded/ready:true`，新窗口和同一工程/页通过核对。 |
| `project find --project <目标UUID> --name <完整名称> --team <团队UUID> --timeout 90s` | 14:35:26 | 19.382 秒 / 0 | `find-team-90s.json`；52/52 可读，无 unreadable，目标 UUID 唯一匹配。 |
| 安装修复后的 CLI，不传 `--timeout` 重复上一步 | 14:37:17 | 19.629 秒 / 0 | `find-team-default-after-install.json`；同样完整 52/52、同一匹配，`seqAbandoned:0`。 |
| `project info --project <目标UUID>` | 见原始 JSON | 退出 0 | `final-project-info.json`；收尾工程和 P3 context 正确。 |

两条尝试错误也保留：`reload.json` 中 Agent 少写了工程 UUID 的两个字符，路由立即拒绝，
随后纠正，未刷新错误工程；`find-team-default.json` 中直接覆盖本机可执行文件后进程被
SIGKILL（退出 -9），改为安装到新文件再原子替换后恢复。此项为本机安装故障，不计为 EDA
查找超时；具体 OS 杀进程原因未取证。上述表中的默认预算复测是在修复安装后完成。

## 修复与验证

- Cobra `project find --timeout` 默认 90 秒，范围 5 秒至 10 分钟；非法范围在派发前拒绝。
- 只调整此查找命令的等待预算，等待结束仍按原有完整性判据返回 `found/absent/unknown`。
  未改连接器枚举与逐项读取算法；未重建或轮换插件。
- 回归测试核对默认/显式预算进入 HTTP `timeoutMs`，无团队/有团队 payload，以及非法预算
  在网络调用前拒绝。`make test`、`make skill-check` 和 diff 空白检查通过。

## 归因与清理

**已证实我们配置的等待预算不足以覆盖当前团队扫描。** 两次正常扫描都比旧 18 秒有效预算
长约 1.4–1.6 秒；增加预算后完整返回，未发现本次扫描中的官方读取失败。不带团队的空清单
仍只表示 `unknown`，不能证明不存在，也不要求盲目反复刷新。

未创建、打开或删除工程，没有新增设计对象或待清理对象，最终工程和 P3 保持可读。
下一次 B03 单项继续核对 create/open/export 及可清理前置条件；其他 Bxx 状态不由本报告改变。
